# CI Skip Dummy Test

This file exists only to open a docs-only PR against `skip-ci-on-text-file-changes`
and verify that the `detect-changes` gate skips the heavy CI jobs while the required
status checks still report success. Delete it once the behavior is confirmed.
