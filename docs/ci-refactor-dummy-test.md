# CI Refactor Dummy Test (docs-only)

This file exists only to open a docs-only PR against `ci-refactor` and verify that the
`classify` gate marks it as a documentation change: `Docs CI Done` runs while `Code CI Done`
passes without dispatching any build. Delete it once the required checks are confirmed.
