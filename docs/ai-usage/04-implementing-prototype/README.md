# Phase 4: Implementing the prototype

Conversations that supported building the out-of-core integration into
NebulaStream: designing the `WindowSlicesStoreInterface` extension, drafting
the UML class diagram, working through C++ code structure, and shaping the
dual-threshold memory-management scheme.

Add new entries as `YYYY-MM-DD-short-topic.md` using [`../_template.md`](../_template.md).
