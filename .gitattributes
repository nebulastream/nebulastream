# Treat binary files as binary to prevent CRLF corruption and bad merges
*.onnx binary
*.png binary
*.zip binary
*.nes binary
*.bin binary

# conn-test config-validation files are NebulaStream SQL-dialect snippets;
# render them with SQL highlighting on GitHub.
*.nesql linguist-language=SQL
