cache: ccache
language: ruby

services:
        - docker

before_install:
        - docker pull nebulastream/nes-build-image:latest 
script:
        - docker run -v /home/travis/build/nebulastream/IoTDB/iotdb:/iotdb --entrypoint /iotdb/docker/scripts/entrypoint-nes-build.sh nebulastream/nes-build-image

notifications:
  email: false
