---
phase: 02-configuration-panels
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - nes-topology-editor/src/components/Sidebar/Sidebar.tsx
  - nes-topology-editor/src/components/Sidebar/PropertiesPanel.tsx
  - nes-topology-editor/src/components/Sidebar/WorkerPanel.tsx
  - nes-topology-editor/src/components/Sidebar/SourcePanel.tsx
  - nes-topology-editor/src/components/Sidebar/SinkPanel.tsx
  - nes-topology-editor/src/components/Sidebar/ConfigForm.tsx
  - nes-topology-editor/src/components/Sidebar/LogicalSourceSelect.tsx
  - nes-topology-editor/src/components/Sidebar/WorkerPanel.test.tsx
  - nes-topology-editor/src/components/Sidebar/SourcePanel.test.tsx
  - nes-topology-editor/src/components/Sidebar/SinkPanel.test.tsx
  - nes-topology-editor/src/App.tsx
autonomous: true
requirements:
  - PROP-01
  - PROP-02
  - PROP-03
  - PROP-04
  - SRCE-03

must_haves:
  truths:
    - "Clicking a worker node opens a sidebar showing host, grpc, capacity fields"
    - "Clicking a source node opens a sidebar showing logical source selector, source type dropdown, type-specific config fields, and parser config"
    - "Clicking a sink node opens a sidebar showing sink type dropdown and type-specific config fields"
    - "Physical source panel has autocomplete field for linking to logical sources"
    - "Clicking empty canvas or pressing Escape closes the sidebar"
    - "Sidebar has two tabs: Properties and Sources"
  artifacts:
    - path: "nes-topology-editor/src/components/Sidebar/Sidebar.tsx"
      provides: "Resizable sidebar container with tab switching"
      exports: ["default"]
    - path: "nes-topology-editor/src/components/Sidebar/PropertiesPanel.tsx"
      provides: "Type-discriminated panel routing"
    - path: "nes-topology-editor/src/components/Sidebar/WorkerPanel.tsx"
      provides: "Worker property form"
    - path: "nes-topology-editor/src/components/Sidebar/SourcePanel.tsx"
      provides: "Physical source config form with logical source link"
    - path: "nes-topology-editor/src/components/Sidebar/SinkPanel.tsx"
      provides: "Sink config form"
    - path: "nes-topology-editor/src/components/Sidebar/ConfigForm.tsx"
      provides: "Generic form renderer from FormFieldDef arrays"
    - path: "nes-topology-editor/src/components/Sidebar/LogicalSourceSelect.tsx"
      provides: "Autocomplete for logical source selection"
    - path: "nes-topology-editor/src/components/Sidebar/WorkerPanel.test.tsx"
      provides: "Worker panel render and field tests"
    - path: "nes-topology-editor/src/components/Sidebar/SourcePanel.test.tsx"
      provides: "Source panel render and field tests"
    - path: "nes-topology-editor/src/components/Sidebar/SinkPanel.test.tsx"
      provides: "Sink panel render and field tests"
  key_links:
    - from: "nes-topology-editor/src/components/Sidebar/PropertiesPanel.tsx"
      to: "nes-topology-editor/src/store/index.ts"
      via: "useStore(s => s.selectedNodeId) for panel routing"
      pattern: "selectedNodeId"
    - from: "nes-topology-editor/src/components/Sidebar/ConfigForm.tsx"
      to: "nes-topology-editor/src/lib/sourceConfigs.ts"
      via: "FormFieldDef[] rendering"
      pattern: "FormFieldDef"
    - from: "nes-topology-editor/src/components/Sidebar/SourcePanel.tsx"
      to: "nes-topology-editor/src/store/topologySlice.ts"
      via: "updatePhysicalSource action"
      pattern: "updatePhysicalSource"
---

<objective>
Build the right sidebar with Properties tab containing type-specific panels for workers, sources, and sinks.

Purpose: Users need to configure node properties after placing them on the canvas. This is the primary data-entry interface.
Output: Sidebar component with tabbed layout, three node-type panels, reusable ConfigForm, LogicalSourceSelect, and panel tests.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-configuration-panels/02-CONTEXT.md
@.planning/phases/02-configuration-panels/02-RESEARCH.md
@.planning/phases/02-configuration-panels/02-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan consumes -->

From nes-topology-editor/src/lib/sourceConfigs.ts (created by Plan 01):
```typescript
export interface FormFieldDef {
  key: string;
  label: string;
  type: 'text' | 'number' | 'select' | 'textarea' | 'boolean';
  required?: boolean;
  defaultValue?: string;
  options?: { value: string; label: string }[];
  placeholder?: string;
}
export const SOURCE_CONFIGS: Record<string, FormFieldDef[]>;
export const SINK_CONFIGS: Record<string, FormFieldDef[]>;
export const PARSER_CONFIG: FormFieldDef[];
export const SOURCE_TYPES: string[];
export const SINK_TYPES: string[];
```

From nes-topology-editor/src/store/topologySlice.ts (updated by Plan 01):
```typescript
updateWorker: (id: string, updates: Partial<Omit<Worker, 'id'>>) => void;
updatePhysicalSource: (id: string, updates: Partial<Omit<PhysicalSource, 'id'>>) => void;
updateSink: (id: string, updates: Partial<Omit<Sink, 'id'>>) => void;
```

From nes-topology-editor/src/store/uiSlice.ts (updated by Plan 01):
```typescript
sidebarTab: 'properties' | 'sources';
selectedQueryId: string | null;
setSidebarTab: (tab: 'properties' | 'sources') => void;
selectQuery: (id: string | null) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create stub components and panel tests (red phase)</name>
  <files>
    nes-topology-editor/src/components/Sidebar/WorkerPanel.tsx,
    nes-topology-editor/src/components/Sidebar/SourcePanel.tsx,
    nes-topology-editor/src/components/Sidebar/SinkPanel.tsx,
    nes-topology-editor/src/components/Sidebar/ConfigForm.tsx,
    nes-topology-editor/src/components/Sidebar/LogicalSourceSelect.tsx,
    nes-topology-editor/src/components/Sidebar/WorkerPanel.test.tsx,
    nes-topology-editor/src/components/Sidebar/SourcePanel.test.tsx,
    nes-topology-editor/src/components/Sidebar/SinkPanel.test.tsx
  </files>
  <behavior>
    - WorkerPanel renders without crashing when given a valid Worker
    - WorkerPanel renders host, grpc, capacity input fields with correct initial values
    - WorkerPanel shows "Connected Sources" and "Connected Sinks" sections
    - SourcePanel renders without crashing when given a valid PhysicalSource
    - SourcePanel renders source type dropdown with all SOURCE_TYPES
    - SourcePanel renders logical source select field
    - SourcePanel renders ConfigForm fields matching the source type config
    - SinkPanel renders without crashing when given a valid Sink
    - SinkPanel renders sink type dropdown with all SINK_TYPES
    - SinkPanel renders sink name input
    - SinkPanel shows "No configuration required" for Void type
  </behavior>
  <action>
    Create minimal stub components so tests can import them, then write tests that describe expected behavior. Tests will fail (red) because stubs return placeholder markup.

    1. **Stub components** (minimal exports so tests compile and imports resolve):
       - `WorkerPanel.tsx`: `export default function WorkerPanel({ worker }: { worker: Worker }) { return <div>WorkerPanel stub</div>; }`
       - `SourcePanel.tsx`: `export default function SourcePanel({ source }: { source: PhysicalSource }) { return <div>SourcePanel stub</div>; }`
       - `SinkPanel.tsx`: `export default function SinkPanel({ sink }: { sink: Sink }) { return <div>SinkPanel stub</div>; }`
       - `ConfigForm.tsx`: `export default function ConfigForm({ fields, values, onChange }: { fields: FormFieldDef[], values: Record<string,string>, onChange: (key: string, value: string) => void }) { return <div>ConfigForm stub</div>; }`
       - `LogicalSourceSelect.tsx`: `export default function LogicalSourceSelect({ value, onChange }: { value: string, onChange: (id: string) => void }) { return <div>LogicalSourceSelect stub</div>; }`
       - Import correct types from `../../lib/types` and `../../lib/sourceConfigs`

    2. **WorkerPanel.test.tsx**:
       - Mock zustand store using established vi.hoisted() pattern from Phase 1
       - Provide a mock Worker: `{ id: 'w1', host: '127.0.0.1', grpc: '4000', capacity: 8, downstream: [], position: { x: 0, y: 0 } }`
       - Test: renders without crash
       - Test: renders input fields with labels "Host Address", "gRPC Address", "Capacity"
       - Test: input fields have correct initial values from worker prop
       - Test: shows "Connected Sources" and "Connected Sinks" headings

    3. **SourcePanel.test.tsx**:
       - Mock zustand store with vi.hoisted() pattern
       - Provide a mock PhysicalSource: `{ id: 's1', logicalSourceId: '', hostWorkerId: 'w1', type: 'Generator', sourceConfig: {}, parserConfig: {}, position: { x: 0, y: 0 } }`
       - Test: renders without crash
       - Test: renders source type dropdown containing SOURCE_TYPES values
       - Test: renders logical source select area
       - Test: renders source configuration section with ConfigForm fields for Generator type

    4. **SinkPanel.test.tsx**:
       - Mock zustand store with vi.hoisted() pattern
       - Provide a mock Sink: `{ id: 'k1', name: 'test-sink', hostWorkerId: 'w1', type: 'File', schema: [], config: {}, position: { x: 0, y: 0 } }`
       - Test: renders without crash
       - Test: renders sink name input with initial value
       - Test: renders sink type dropdown containing SINK_TYPES values
       - Test: Void sink type shows "No configuration required" text
  </action>
  <verify>
    <automated>cd nes-topology-editor && npx vitest run src/components/Sidebar/WorkerPanel.test.tsx src/components/Sidebar/SourcePanel.test.tsx src/components/Sidebar/SinkPanel.test.tsx --reporter=verbose 2>&1; echo "Exit: $?"</automated>
  </verify>
  <done>Test files exist and run (expected to fail/red) for WorkerPanel, SourcePanel, SinkPanel covering PROP-01, PROP-02, PROP-03, PROP-04, SRCE-03. Stub components exist so imports resolve. Tests are in red state awaiting full component implementation in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Create Sidebar container, ConfigForm, LogicalSourceSelect, and type-specific panels (green phase)</name>
  <files>
    nes-topology-editor/src/components/Sidebar/Sidebar.tsx,
    nes-topology-editor/src/components/Sidebar/ConfigForm.tsx,
    nes-topology-editor/src/components/Sidebar/LogicalSourceSelect.tsx,
    nes-topology-editor/src/components/Sidebar/PropertiesPanel.tsx,
    nes-topology-editor/src/components/Sidebar/WorkerPanel.tsx,
    nes-topology-editor/src/components/Sidebar/SourcePanel.tsx,
    nes-topology-editor/src/components/Sidebar/SinkPanel.tsx,
    nes-topology-editor/src/App.tsx,
    nes-topology-editor/src/App.css
  </files>
  <action>
    Replace stub components with full implementations and wire everything together. All tests from Task 1 must pass (green) after this task.

    1. **Sidebar.tsx**: Create the sidebar container component:
       - Resizable by dragging the left edge (per user decision). Use CSS + mouse events pattern from research: `onMouseDown` on a 4px resize handle div on the left edge. Min width 280px, max 600px, default 360px.
       - Two tabs at top: "Properties" and "Sources" (using `sidebarTab` from uiSlice, `setSidebarTab` action)
       - Tab styling: bottom-border highlight on active tab, gray text on inactive. Use Tailwind classes.
       - When `sidebarTab === 'properties'`: render `<PropertiesPanel />`
       - When `sidebarTab === 'sources'`: render a placeholder div with text "Logical sources panel" (Plan 03 replaces this)
       - Sidebar is always visible (tabs provide navigation even without selected node)
       - Export default

    2. **ConfigForm.tsx**: Replace stub with full generic form renderer (PROP-04):
       - Props: `{ fields: FormFieldDef[], values: Record<string, string>, onChange: (key: string, value: string) => void }`
       - For each `FormFieldDef`, render the appropriate input:
         - `'text'`: `<input type="text" />` with label, placeholder
         - `'number'`: `<input type="number" />` with label, placeholder
         - `'select'`: `<select>` with options from field.options
         - `'textarea'`: `<textarea>` with label, placeholder, 3 rows
         - `'boolean'`: `<input type="checkbox" />` (value "true"/"false" as string)
       - Show red asterisk on required fields
       - All inputs use Tailwind: `w-full px-2 py-1.5 border border-gray-300 rounded text-sm focus:outline-none focus:ring-1 focus:ring-blue-500`
       - Labels: `text-xs font-medium text-gray-600 mb-1`
       - Each field wrapped in `<div className="mb-3">`

    3. **LogicalSourceSelect.tsx**: Replace stub with full autocomplete component:
       - Props: `{ value: string, onChange: (logicalSourceId: string) => void }`
       - Reads `logicalSources` from store via `useStore`
       - Text input that filters logical sources by name as user types
       - Dropdown list appears below input when focused and filter is non-empty
       - Clicking an item sets the value and closes dropdown
       - If the current `value` (logicalSourceId) does not match any logical source, show a warning badge: yellow background, "Missing logical source" text (orphan indicator per user decision)
       - Use absolute positioning + z-10 for the dropdown

    4. **PropertiesPanel.tsx**: Type-discriminated panel router:
       - Read `selectedNodeId` from store
       - Look up the selected node across workers, physicalSources, sinks arrays
       - If worker found: render `<WorkerPanel worker={worker} />`
       - If source found: render `<SourcePanel source={source} />`
       - If sink found: render `<SinkPanel sink={sink} />`
       - If nothing selected: render empty state -- centered gray text "Select a node to edit its properties" with a mouse-pointer icon from lucide-react

    5. **WorkerPanel.tsx**: Replace stub with full worker property form (PROP-01):
       - Props: `{ worker: Worker }`
       - Header: "Worker" with worker.id truncated
       - Fields (using direct inputs, not ConfigForm -- workers have simple fixed fields):
         - `host`: text input, label "Host Address"
         - `grpc`: text input, label "gRPC Address"
         - `capacity`: number input, label "Capacity"
       - Each field change calls `useStore.getState().updateWorker(worker.id, { [field]: value })` (use getState to avoid stale closures)
       - Below fields, show "Connected Sources" and "Connected Sinks" sections: list names/ids of physicalSources and sinks where hostWorkerId matches this worker. Read-only display (informational, per PROP-01).

    6. **SourcePanel.tsx**: Replace stub with full physical source config form (PROP-02, PROP-04, SRCE-03):
       - Props: `{ source: PhysicalSource }`
       - Header: "Source" with source type badge
       - Section 1 -- "Logical Source": `<LogicalSourceSelect value={source.logicalSourceId} onChange={...} />`
         - onChange calls `updatePhysicalSource(source.id, { logicalSourceId: newId })`
       - Section 2 -- "Source Type": dropdown of SOURCE_TYPES. Changing type resets sourceConfig to defaults from SOURCE_CONFIGS.
         - onChange calls `updatePhysicalSource(source.id, { type: newType, sourceConfig: buildDefaults(SOURCE_CONFIGS[newType]) })`
         - Helper `buildDefaults(fields: FormFieldDef[]): Record<string,string>` creates object from defaultValue fields
       - Section 3 -- "Source Configuration": `<ConfigForm fields={SOURCE_CONFIGS[source.type]} values={source.sourceConfig} onChange={...} />`
         - onChange calls `updatePhysicalSource(source.id, { sourceConfig: { ...source.sourceConfig, [key]: value } })`
       - Section 4 -- "Parser Configuration" (collapsible): `<ConfigForm fields={PARSER_CONFIG} values={source.parserConfig} onChange={...} />`
         - onChange calls `updatePhysicalSource(source.id, { parserConfig: { ...source.parserConfig, [key]: value } })`

    7. **SinkPanel.tsx**: Replace stub with full sink config form (PROP-03, PROP-04):
       - Props: `{ sink: Sink }`
       - Header: "Sink" with sink name or type badge
       - Section 1 -- "Sink Name": text input for sink.name
       - Section 2 -- "Sink Type": dropdown of SINK_TYPES. Changing type resets config to defaults.
       - Section 3 -- "Configuration": `<ConfigForm fields={SINK_CONFIGS[sink.type]} values={sink.config} onChange={...} />`
         - For Void sink type: show "No configuration required" text instead of empty form
       - onChange handlers follow same pattern as SourcePanel

    8. **App.tsx**: Replace the sidebar placeholder div from Plan 01 with `<Sidebar />` import. The sidebar should always be visible (remove the selectedNodeId conditional -- the sidebar has tabs that work regardless of selection). Keep the bottom panel placeholder.

    9. **App.css**: Remove the `.sidebar-placeholder` class (replaced by Sidebar component). Keep other styles.
  </action>
  <verify>
    <automated>cd nes-topology-editor && npx vitest run src/components/Sidebar/WorkerPanel.test.tsx src/components/Sidebar/SourcePanel.test.tsx src/components/Sidebar/SinkPanel.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>Clicking a worker shows host/grpc/capacity fields. Clicking a source shows logical source selector + type-specific config. Clicking a sink shows type + config. Empty state shown when no node selected. Sidebar integrated in App layout. All panel tests from Task 1 now pass (green). ConfigForm renders typed inputs for all NES source/sink configs (PROP-04).</done>
</task>

</tasks>

<verification>
- `cd nes-topology-editor && npx vitest run --reporter=verbose` -- all tests pass
- `cd nes-topology-editor && npx tsc --noEmit` -- no type errors
- Sidebar renders with Properties/Sources tabs
- Worker, Source, Sink panels display correct fields based on node type
- ConfigForm renders all NES source/sink field definitions
- LogicalSourceSelect shows autocomplete with orphan warning
- Panel test files provide automated verification for PROP-01, PROP-02, PROP-03, PROP-04, SRCE-03
</verification>

<success_criteria>
- Clicking a worker node shows host, grpc, capacity fields in sidebar
- Clicking a source node shows logical source link, source type, source config, parser config
- Clicking a sink node shows name, sink type, sink config
- Physical source has autocomplete for logical source selection with orphan warning
- Sidebar has Properties and Sources tabs
- Sidebar is resizable by dragging left edge
- WorkerPanel.test.tsx, SourcePanel.test.tsx, SinkPanel.test.tsx all pass
- All existing + new tests pass, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-panels/02-02-SUMMARY.md`
</output>
