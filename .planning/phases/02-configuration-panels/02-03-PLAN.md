---
phase: 02-configuration-panels
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - nes-topology-editor/src/components/Sidebar/LogicalSourcesPanel.tsx
  - nes-topology-editor/src/components/Sidebar/SchemaBuilder.tsx
  - nes-topology-editor/src/components/Sidebar/LogicalSourcesPanel.test.tsx
  - nes-topology-editor/src/components/Sidebar/SchemaBuilder.test.tsx
  - nes-topology-editor/src/components/Sidebar/Sidebar.tsx
autonomous: true
requirements:
  - SRCE-01
  - SRCE-02
  - PROP-05

must_haves:
  truths:
    - "User can create a new logical source with a name"
    - "User can edit a logical source name"
    - "User can delete a logical source"
    - "User can add schema fields (name + type) to a logical source"
    - "User can remove schema fields from a logical source"
    - "Type dropdown shows all 13 NES native types"
    - "Deleting a logical source does not delete physical sources that reference it"
  artifacts:
    - path: "nes-topology-editor/src/components/Sidebar/LogicalSourcesPanel.tsx"
      provides: "Logical source CRUD list"
      exports: ["default"]
    - path: "nes-topology-editor/src/components/Sidebar/SchemaBuilder.tsx"
      provides: "Inline schema table with add/remove fields"
      exports: ["default"]
  key_links:
    - from: "nes-topology-editor/src/components/Sidebar/LogicalSourcesPanel.tsx"
      to: "nes-topology-editor/src/store/topologySlice.ts"
      via: "addLogicalSource, updateLogicalSource, removeLogicalSource"
      pattern: "addLogicalSource|updateLogicalSource|removeLogicalSource"
    - from: "nes-topology-editor/src/components/Sidebar/SchemaBuilder.tsx"
      to: "nes-topology-editor/src/lib/types.ts"
      via: "SchemaField type"
      pattern: "SchemaField"
---

<objective>
Build the logical source management panel and schema builder for the Sources tab.

Purpose: Logical sources are a first-class concept in NES -- users need dedicated UI to create, name, and define schemas before linking physical sources.
Output: LogicalSourcesPanel with CRUD and inline SchemaBuilder component, wired into Sidebar Sources tab.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-configuration-panels/02-CONTEXT.md
@.planning/phases/02-configuration-panels/02-RESEARCH.md
@.planning/phases/02-configuration-panels/02-01-SUMMARY.md
@.planning/phases/02-configuration-panels/02-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 -->

From nes-topology-editor/src/lib/types.ts:
```typescript
export interface SchemaField { name: string; type: string; }
export interface LogicalSource { id: string; name: string; schema: SchemaField[]; }
```

From nes-topology-editor/src/store/topologySlice.ts (updated by Plan 01):
```typescript
addLogicalSource: (name: string) => void;
updateLogicalSource: (id: string, updates: Partial<Omit<LogicalSource, 'id'>>) => void;
removeLogicalSource: (id: string) => void;
```

From nes-topology-editor/src/store/uiSlice.ts (updated by Plan 01):
```typescript
sidebarTab: 'properties' | 'sources';
setSidebarTab: (tab: 'properties' | 'sources') => void;
```

<!-- Contract from Plan 02: Sidebar.tsx exists with Sources tab placeholder -->
From nes-topology-editor/src/components/Sidebar/Sidebar.tsx (created by Plan 02):
```typescript
// Sources tab currently renders placeholder text "Logical sources panel"
// This plan replaces that placeholder with <LogicalSourcesPanel />
```

NES native types (from CONTEXT.md decision):
INT8, INT16, INT32, INT64, UINT8, UINT16, UINT32, UINT64, FLOAT32, FLOAT64, BOOLEAN, CHAR, TEXT
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SchemaBuilder component</name>
  <files>
    nes-topology-editor/src/components/Sidebar/SchemaBuilder.tsx,
    nes-topology-editor/src/components/Sidebar/SchemaBuilder.test.tsx
  </files>
  <behavior>
    - Renders a row for each existing schema field with name input and type dropdown
    - Clicking "+" adds a new field with empty name and default type INT32
    - Clicking "-" on a row removes that field
    - Editing field name updates the field at that index
    - Changing type dropdown updates the field at that index
    - Type dropdown contains all 13 NES types
    - Empty state shows "No fields defined" with the add button
  </behavior>
  <action>
    1. **SchemaBuilder.tsx**:
       - Props: `{ fields: SchemaField[], onChange: (fields: SchemaField[]) => void }`
       - Constant `NES_TYPES` array: `['INT8', 'INT16', 'INT32', 'INT64', 'UINT8', 'UINT16', 'UINT32', 'UINT64', 'FLOAT32', 'FLOAT64', 'BOOLEAN', 'CHAR', 'TEXT']`
       - Render each field as a row: `<input>` for name (flex-1), `<select>` for type, `-` button to remove
       - `+` Add field button below the rows, creates `{ name: '', type: 'INT32' }`
       - Tailwind styling: compact rows with gap-2, text-sm inputs, red `-` button, blue `+` button
       - Empty state: centered gray text "No fields defined" with the `+` button

    2. **SchemaBuilder.test.tsx**:
       - Use `@testing-library/react` to render SchemaBuilder with mock onChange
       - Test: renders existing fields with correct name and type values
       - Test: clicking add button calls onChange with new field appended
       - Test: clicking remove button calls onChange with field removed
       - Test: typing in name input calls onChange with updated field
       - Test: changing type dropdown calls onChange with updated type
       - Test: all 13 NES types appear in type dropdown
  </action>
  <verify>
    <automated>cd nes-topology-editor && npx vitest run src/components/Sidebar/SchemaBuilder.test.tsx --reporter=verbose</automated>
  </verify>
  <done>SchemaBuilder renders fields, supports add/remove/edit, shows all NES types, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LogicalSourcesPanel and wire to Sidebar Sources tab</name>
  <files>
    nes-topology-editor/src/components/Sidebar/LogicalSourcesPanel.tsx,
    nes-topology-editor/src/components/Sidebar/LogicalSourcesPanel.test.tsx,
    nes-topology-editor/src/components/Sidebar/Sidebar.tsx
  </files>
  <behavior>
    - Panel lists all logical sources from the store
    - Clicking "New Source" creates a logical source with auto-generated name
    - Each logical source shows its name (editable) and schema builder
    - Clicking delete on a logical source removes it from the store
    - Schema changes call updateLogicalSource with new schema array
    - Empty state shows "No logical sources defined" with create button
  </behavior>
  <action>
    1. **LogicalSourcesPanel.tsx**:
       - Read `logicalSources` from store
       - Top: "Logical Sources" header + "New Source" button (lucide Plus icon)
       - Clicking "New Source" calls `addLogicalSource('Source_' + (logicalSources.length + 1))`
       - Each logical source rendered as a card/section:
         - Editable name input at top (calls `updateLogicalSource(id, { name })`)
         - Delete button (lucide Trash2 icon) -- calls `removeLogicalSource(id)`
         - `<SchemaBuilder fields={ls.schema} onChange={(schema) => updateLogicalSource(ls.id, { schema })} />`
       - Card styling: border, rounded-lg, p-3, mb-3, bg-white
       - Empty state: centered text "No logical sources defined" + create button

    2. **Sidebar.tsx**: Update the Sources tab to render `<LogicalSourcesPanel />` instead of the placeholder text "Logical sources panel". Import LogicalSourcesPanel. Plan 02 has already created Sidebar.tsx with the placeholder, so this is a straightforward replacement.

    3. **LogicalSourcesPanel.test.tsx**:
       - Mock zustand store using the established vi.hoisted() pattern from Phase 1
       - Test: renders empty state when no logical sources
       - Test: renders logical source names when sources exist
       - Test: clicking "New Source" calls addLogicalSource
       - Test: clicking delete calls removeLogicalSource
       - Test: editing name calls updateLogicalSource
  </action>
  <verify>
    <automated>cd nes-topology-editor && npx vitest run src/components/Sidebar/LogicalSourcesPanel.test.tsx src/components/Sidebar/SchemaBuilder.test.tsx --reporter=verbose</automated>
  </verify>
  <done>LogicalSourcesPanel shows CRUD for logical sources with inline schema builder. Tests pass. Sources tab in sidebar shows LogicalSourcesPanel.</done>
</task>

</tasks>

<verification>
- `cd nes-topology-editor && npx vitest run --reporter=verbose` -- all tests pass
- `cd nes-topology-editor && npx tsc --noEmit` -- no type errors
- Logical sources can be created, edited, deleted
- Schema fields can be added, removed, edited with NES type dropdown
</verification>

<success_criteria>
- LogicalSourcesPanel renders in Sources tab of sidebar
- User can create new logical sources with auto-generated names
- User can edit logical source names inline
- User can delete logical sources (orphan policy -- no cascade)
- SchemaBuilder shows inline table of field name + type
- All 13 NES types available in type dropdown
- Schema fields can be added (+) and removed (-)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-panels/02-03-SUMMARY.md`
</output>
