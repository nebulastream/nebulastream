---
phase: 02-configuration-panels
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04"]
files_modified: []
autonomous: false
requirements:
  - PROP-01
  - PROP-02
  - PROP-03
  - PROP-04
  - PROP-05
  - SRCE-01
  - SRCE-02
  - SRCE-03
  - QURY-01
  - QURY-02
  - QURY-03

must_haves:
  truths:
    - "All phase 2 requirements verified by human interaction"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 2 configuration panels.

Purpose: Confirm that all property panels, logical source management, and query editor work correctly in the browser.
Output: Verification pass or list of issues to address in gap closure plans.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-configuration-panels/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all Phase 2 configuration panels in browser</name>
  <files></files>
  <action>
    Start the dev server and run through all 7 test groups below. This is a human verification checkpoint -- no code changes needed.

    Complete Phase 2 configuration panels built:
    1. Right sidebar with Properties tab (worker/source/sink panels) and Sources tab (logical source CRUD + schema builder)
    2. Bottom panel with query tab list and Monaco SQL editor
    3. Resizable sidebar and bottom panel
    4. Type-specific config forms for all NES source types (Generator, File, TCP) and sink types (File, Print, Void, Checksum)
    5. Logical source autocomplete with orphan warning badge

    Start the dev server: `cd nes-topology-editor && npm run dev`
    Open http://localhost:5173 in your browser.

    **Test 1: Worker Panel (PROP-01)**
    1. Click the "Add Worker" button in toolbar to place a worker
    2. Click on the worker node -- sidebar should show Properties tab with host, gRPC, capacity fields
    3. Edit the host field -- value should persist
    4. Verify the "Connected Sources" and "Connected Sinks" sections appear (empty for now)

    **Test 2: Source Panel (PROP-02, PROP-04, SRCE-03)**
    1. Click "Add Source" to place a source
    2. Drag the source onto a worker to attach it
    3. Click the source node -- sidebar should show source panel
    4. Change the source type dropdown (Generator/File/TCP) -- form fields should update
    5. Fill in some Generator config fields (seed, generator_schema) -- values should persist
    6. Check "Parser Configuration" section is visible
    7. Check "Logical Source" field -- it should be an autocomplete (empty for now)

    **Test 3: Sink Panel (PROP-03, PROP-04)**
    1. Click "Add Sink" to place a sink
    2. Click the sink node -- sidebar should show sink panel
    3. Change sink type (File/Print/Void/Checksum) -- form fields should update
    4. For Void type, should show "No configuration required"
    5. Edit sink name -- value should persist

    **Test 4: Logical Sources (SRCE-01, SRCE-02, PROP-05)**
    1. Click the "Sources" tab in the sidebar
    2. Click "New Source" button -- a new logical source card should appear
    3. Edit the logical source name
    4. Click "+" in the schema builder to add a field
    5. Type a field name and select a type from the dropdown
    6. Add a second field, then remove the first with "-"
    7. Create a second logical source
    8. Delete the first logical source -- it should disappear
    9. Switch to Properties tab, click a source node, verify the logical source autocomplete shows the remaining logical source

    **Test 5: Query Editor (QURY-01, QURY-02, QURY-03)**
    1. Look at the bottom panel -- should show empty state
    2. Click "+" to add a query -- a new tab should appear as "(unnamed)"
    3. Type SQL in the editor: `SELECT * FROM source1 INTO sink1` -- syntax highlighting should work (keywords colored)
    4. Double-click the tab label to rename the query
    5. Add a second query tab
    6. Click between tabs -- editor content should switch
    7. Click "X" on a tab to remove a query
    8. Try collapsing and expanding the bottom panel
    9. Try resizing the bottom panel by dragging the top edge

    **Test 6: Sidebar Resize**
    1. Hover over the left edge of the sidebar -- cursor should change to resize cursor
    2. Drag to resize the sidebar -- it should grow/shrink within min/max bounds

    **Test 7: Panel Switching**
    1. Click different nodes (worker, source, sink) -- sidebar should switch content
    2. Click empty canvas -- sidebar should show "Select a node" empty state
  </action>
  <verify>Human confirms all 7 test groups pass. Type "approved" or describe any issues found.</verify>
  <done>All Phase 2 requirements verified working in browser, or issues documented for gap closure.</done>
</task>

</tasks>

<verification>
Human confirms all 7 test groups pass in the browser.
</verification>

<success_criteria>
- All property panels render correctly for each node type
- Logical source CRUD and schema builder work as expected
- Query editor has syntax highlighting and tab management
- Resize and collapse UX is functional
- No visual glitches or broken interactions
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-panels/02-05-SUMMARY.md`
</output>
