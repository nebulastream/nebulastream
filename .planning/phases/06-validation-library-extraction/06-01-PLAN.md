---
phase: 06-validation-library-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # NOTE: 12 files but 9 are mechanical stub headers (< 20 lines each). Actual complexity is low.
  - nes-validator/CMakeLists.txt
  - nes-validator/include/Validator/TopologyValidator.hpp
  - nes-validator/src/TopologyValidator.cpp
  - nes-validator/src/YamlBinding.hpp
  - nes-validator/src/YamlBinding.cpp
  - nes-validator/stubs/folly/hash/Hash.h
  - nes-validator/stubs/cpptrace/basic.hpp
  - nes-validator/stubs/cpptrace/cpptrace.hpp
  - nes-validator/stubs/cpptrace/from_current.hpp
  - nes-validator/tests/CMakeLists.txt
  - nes-validator/tests/UnitTests/TopologyValidatorTest.cpp
  - CMakeLists.txt
autonomous: true
requirements: [VLIB-01, VLIB-02]

must_haves:
  truths:
    - "nes-validator CMake target compiles without gRPC, Folly, cpptrace runtime dependencies"
    - "Stub headers for folly/hash/Hash.h and cpptrace/* are resolved before system headers"
    - "TopologyValidator.hpp declares validateTopology(const std::string&) public API"
    - "Unit test target registers and links against nes-validator"
    - "ANTLR4 C++ runtime comes via vcpkg (transitively through nes-sql-parser), not FetchContent"
  artifacts:
    - path: "nes-validator/CMakeLists.txt"
      provides: "CMake target definition with stub include paths and NES target dependencies"
      contains: "target_include_directories"
    - path: "nes-validator/include/Validator/TopologyValidator.hpp"
      provides: "Public API header"
      exports: ["validateTopology"]
    - path: "nes-validator/src/TopologyValidator.cpp"
      provides: "Skeleton implementation (stub returning empty string)"
    - path: "nes-validator/stubs/folly/hash/Hash.h"
      provides: "std::hash replacement for folly::hash::hash_combine"
    - path: "nes-validator/stubs/cpptrace/from_current.hpp"
      provides: "CPPTRACE_TRY/CATCH macros as plain try/catch"
    - path: "nes-validator/tests/UnitTests/TopologyValidatorTest.cpp"
      provides: "GTest test file for validation library"
  key_links:
    - from: "nes-validator/CMakeLists.txt"
      to: "nes-sql-parser, nes-logical-operators, nes-data-types"
      via: "target_link_libraries"
      pattern: "target_link_libraries.*nes-sql-parser"
    - from: "nes-validator/CMakeLists.txt"
      to: "nes-validator/stubs/"
      via: "BEFORE include path injection"
      pattern: "BEFORE.*stubs"
    - from: "CMakeLists.txt"
      to: "nes-validator/CMakeLists.txt"
      via: "add_subdirectory"
      pattern: "add_subdirectory.*nes-validator"
---

<objective>
Create the nes-validator CMake target with stub/facade layer, public API skeleton, and test scaffold.

Purpose: Establish the standalone validation library that compiles without heavy NES runtime dependencies (gRPC, Folly, cpptrace). This is the foundation all subsequent plans build on.
Output: Compilable nes-validator target with stubs, skeleton API, and registered test target.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-validation-library-extraction/06-CONTEXT.md
@.planning/phases/06-validation-library-extraction/06-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
<!-- Key types and contracts the executor needs from existing NES codebase -->

From nes-sources/include/Sources/SourceCatalog.hpp:
```cpp
class SourceCatalog {
    [[nodiscard]] std::optional<NES::LogicalSource> addLogicalSource(const std::string& logicalSourceName, const Schema& schema);
    [[nodiscard]] std::expected<SourceDescriptor, Exception> addPhysicalSource(...);
};
```

From nes-sql-parser/include/SQLQueryParser/StatementBinder.hpp:
```cpp
class StatementBinder {
    parseAndBind(std::string_view statementString) const;
    [[nodiscard]] std::expected<Statement, Exception> parseAndBindSingle(std::string_view statementStrings) const;
};
```

From nes-sources/include/Sources/SourceDescriptor.hpp:
- Uses `folly::hash::hash_combine` in std::hash specialization (needs stub)

From nes-common/include/ErrorHandling.hpp:
- Exception class inherits from `cpptrace::lazy_exception` (needs stub)
- Uses `CPPTRACE_TRY`/`CPPTRACE_CATCH` macros (needs stub)

From cmake/NebulaStreamTest.cmake:
- `add_nes_unit_test(name sources...)` macro for test registration

ANTLR4 dependency status (native build):
- ANTLR4 C++ runtime is ALREADY via vcpkg: `find_package(antlr4-runtime CONFIG REQUIRED)` in nes-sql-parser/CMakeLists.txt, `antlr4` in vcpkg/vcpkg.json
- The FetchContent in nes-sql-parser is ONLY for the ANTLR4 JAR file (Java parser generator tool), not the C++ runtime
- nes-validator gets ANTLR4 transitively via nes-sql-parser -> antlr4_static (no FetchContent needed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nes-validator CMake target with stubs</name>
  <files>
    nes-validator/CMakeLists.txt,
    nes-validator/include/Validator/TopologyValidator.hpp,
    nes-validator/src/TopologyValidator.cpp,
    nes-validator/stubs/folly/hash/Hash.h,
    nes-validator/stubs/cpptrace/basic.hpp,
    nes-validator/stubs/cpptrace/cpptrace.hpp,
    nes-validator/stubs/cpptrace/from_current.hpp,
    CMakeLists.txt
  </files>
  <action>
    1. Create directory structure: `nes-validator/include/Validator/`, `nes-validator/src/`, `nes-validator/stubs/folly/hash/`, `nes-validator/stubs/cpptrace/`.

    2. Create `nes-validator/include/Validator/TopologyValidator.hpp`:
       - Namespace `NES::Validator`
       - Declare: `std::string validateTopology(const std::string& yamlString);`
       - Include guards, Apache 2.0 license header (match NES convention)

    3. Create `nes-validator/src/TopologyValidator.cpp`:
       - Skeleton implementation that returns `""` (empty string = valid)
       - Include the header
       - This is a placeholder — Plan 02 will implement the full pipeline

    4. Create stub headers (per Research Pattern 4 and 5):

       `nes-validator/stubs/folly/hash/Hash.h`:
       ```cpp
       #pragma once
       #include <functional>
       namespace folly { namespace hash {
       template <typename... Ts>
       size_t hash_combine(const Ts&... args) {
           size_t seed = 0;
           (..., (seed ^= std::hash<Ts>{}(args) + 0x9e3779b9 + (seed << 6) + (seed >> 2)));
           return seed;
       }
       }} // namespace folly::hash
       ```

       `nes-validator/stubs/cpptrace/from_current.hpp`:
       ```cpp
       #pragma once
       #define CPPTRACE_TRY try
       #define CPPTRACE_CATCH(...) catch(...)
       ```

       `nes-validator/stubs/cpptrace/basic.hpp`:
       - Minimal cpptrace namespace with `raw_trace` struct that has a static `current()` returning empty instance

       `nes-validator/stubs/cpptrace/cpptrace.hpp`:
       - Must define `cpptrace::lazy_exception` as a class inheriting from `std::exception` with a `what()` override returning a stored message. This is CRITICAL because NES's `Exception` class inherits from `cpptrace::lazy_exception`. The stub must define:
         - `cpptrace::lazy_exception` base class with constructor accepting a string message
         - `what()` returning the stored message as `const char*`
         - `cpptrace::raw_trace` with static `current(int = 0)` returning empty instance
       - Inspect `nes-common/include/ErrorHandling.hpp` first to understand what cpptrace symbols NES actually uses, then create stubs matching those exact signatures.

    5. Create `nes-validator/CMakeLists.txt`:
       - Define library target `nes-validator` (STATIC)
       - Sources: `src/TopologyValidator.cpp`
       - Public include: `include/`
       - CRITICAL: Use `target_include_directories(nes-validator BEFORE PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/stubs)` to ensure stubs override system headers
       - Link dependencies: `nes-sql-parser`, `nes-logical-operators`, `nes-data-types`, `nes-configurations`
       - For nes-sources and nes-sinks: inspect their CMakeLists.txt to understand the transitive dep chain. If they PUBLIC-link nes-executable/nes-memory/nes-runtime (heavy deps), do NOT link them directly. Instead, add their include directories manually and compile only the specific source files needed (SourceCatalog.cpp, SinkCatalog.cpp) or create an INTERFACE library that exposes only the headers.
       - Use CMake C++23 standard
       - Do NOT link gRPC, Folly, cpptrace, nes-runtime, nes-executable, nes-memory
       - Do NOT add FetchContent for ANTLR4 — it comes transitively via nes-sql-parser (already in vcpkg)

    6. Add `add_subdirectory(nes-validator)` to the root `CMakeLists.txt` (find the appropriate location near other NES subdirectories).

    7. Verify ANTLR4 is NOT pulled via FetchContent for nes-validator:
       ```
       grep -r "FetchContent.*antlr" nes-validator/ || echo "OK: no ANTLR4 FetchContent in nes-validator"
       ```
       The native build should get ANTLR4 entirely from vcpkg via the nes-sql-parser transitive dependency.

    8. Build the target to verify it compiles:
       ```
       cmake --build cmake-build-debug --target nes-validator -j$(nproc)
       ```
       If compilation fails due to missing headers or transitive deps, iteratively fix by:
       - Adding more stub headers
       - Adjusting include paths
       - Investigating which headers pull in problematic deps
       NOTE: The reflectcpp dependency (rfl headers from Reflection.hpp) is a known unknown per the research. If `rfl/Generic.hpp` is not found, either add reflectcpp to the build or create minimal rfl stubs. Try the simplest approach first.
  </action>
  <verify>
    <automated>cd /IdeaProjects/nebulastream2 && cmake --build cmake-build-debug --target nes-validator -j$(nproc) 2>&1 | tail -5</automated>
  </verify>
  <done>nes-validator target compiles successfully without gRPC, Folly, cpptrace runtime libraries. Stubs resolve all heavy dependency headers. ANTLR4 comes via vcpkg (no FetchContent).</done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffold and verify end-to-end build</name>
  <files>
    nes-validator/tests/CMakeLists.txt,
    nes-validator/tests/UnitTests/TopologyValidatorTest.cpp
  </files>
  <action>
    1. Create `nes-validator/tests/CMakeLists.txt`:
       - Use `add_nes_unit_test(topology-validator-test "UnitTests/TopologyValidatorTest.cpp")`
       - Link: `target_link_libraries(topology-validator-test PRIVATE nes-validator)`

    2. Create `nes-validator/tests/UnitTests/TopologyValidatorTest.cpp`:
       - Follow NES test pattern (see `nes-sql-parser/tests/StatementBinderTest.cpp` for reference)
       - Class `TopologyValidatorTest : public Testing::BaseUnitTest`
       - SetUpTestSuite with Logger setup
       - One test: `TEST_F(TopologyValidatorTest, SkeletonReturnsEmptyString)` that calls `validateTopology("")` and expects empty string return (skeleton behavior)
       - This is a scaffold — Plan 02 will add real test cases with YAML fixtures

    3. Add `add_subdirectory(tests)` to `nes-validator/CMakeLists.txt` (guarded by BUILD_TESTING or the NES test guard pattern — check how other NES modules guard their test subdirectories).

    4. Build and run the test:
       ```
       cmake --build cmake-build-debug --target topology-validator-test -j$(nproc)
       ctest --test-dir cmake-build-debug -R "topology-validator-test" --output-on-failure
       ```
  </action>
  <verify>
    <automated>cd /IdeaProjects/nebulastream2 && cmake --build cmake-build-debug --target topology-validator-test -j$(nproc) 2>&1 | tail -3 && ctest --test-dir cmake-build-debug -R "topology-validator-test" --output-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>topology-validator-test target builds and the skeleton test passes. Test infrastructure is ready for Plan 02 to add real test cases.</done>
</task>

</tasks>

<verification>
- `cmake --build cmake-build-debug --target nes-validator -j$(nproc)` succeeds (no gRPC/Folly/cpptrace link errors)
- `ctest --test-dir cmake-build-debug -R "topology-validator-test" --output-on-failure` passes
- `grep -r "folly\|cpptrace\|grpc" nes-validator/CMakeLists.txt` shows no direct links to heavy deps
- `grep -r "FetchContent.*antlr" nes-validator/` returns nothing (ANTLR4 via vcpkg only)
- Stubs directory contains folly/hash/Hash.h and cpptrace/*.hpp
</verification>

<success_criteria>
- nes-validator CMake target compiles as a standalone static library
- No runtime dependency on gRPC, Folly, or cpptrace (stubs handle compile-time needs)
- ANTLR4 comes transitively via nes-sql-parser from vcpkg (no FetchContent in nes-validator)
- TopologyValidator.hpp exports validateTopology() in NES::Validator namespace
- Test scaffold compiles, links, and skeleton test passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-validation-library-extraction/06-01-SUMMARY.md`
</output>
