---
phase: 01-interactive-canvas
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - nes-topology-editor/src/App.tsx
  - nes-topology-editor/src/App.css
  - nes-topology-editor/src/components/Toolbar/Toolbar.tsx
  - nes-topology-editor/src/components/Canvas/Canvas.tsx
  - nes-topology-editor/src/__tests__/palette-dnd.test.tsx
autonomous: true
gap_closure: true
requirements: [CANV-01, CANV-02, CANV-03, CANV-04, CANV-05, CANV-07, CANV-08, CANV-09, CANV-10, CANV-11, CANV-12]

must_haves:
  truths:
    - "Palette sidebar is completely removed from the UI"
    - "Toolbar has Add Source and Add Sink buttons alongside existing Add Worker"
    - "Clicking Add Source creates a source node on the canvas"
    - "Clicking Add Sink creates a sink node on the canvas"
    - "Palette DnD tests are replaced with toolbar-based node creation tests"
  artifacts:
    - path: "nes-topology-editor/src/App.tsx"
      provides: "App layout without Palette import or rendering"
      contains: "Toolbar"
    - path: "nes-topology-editor/src/components/Toolbar/Toolbar.tsx"
      provides: "Toolbar with Add Worker, Add Source, Add Sink buttons"
      contains: "Add Source"
  key_links:
    - from: "nes-topology-editor/src/components/Toolbar/Toolbar.tsx"
      to: "store.addPhysicalSource / store.addSink"
      via: "useStore selector + onClick handlers"
      pattern: "addPhysicalSource|addSink"
---

<objective>
Remove the sidebar Palette component (user decided against drag-from-palette UX) and add toolbar buttons for source/sink creation to maintain CANV-01/CANV-02 coverage.

Purpose: User explicitly decided to remove the palette sidebar during verification. Alternative node creation buttons in the toolbar are needed so users can still add sources and sinks.
Output: Palette removed, toolbar updated with Add Source / Add Sink buttons, tests updated.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-interactive-canvas/01-VERIFICATION.md

<interfaces>
<!-- Current Toolbar structure to extend -->
From nes-topology-editor/src/components/Toolbar/Toolbar.tsx:
```typescript
const addWorker = useStore((s) => s.addWorker);
// Currently only has Add Worker and Auto Layout buttons
```

From nes-topology-editor/src/store/topologySlice.ts:
```typescript
addPhysicalSource: (source: Omit<PhysicalSource, 'id'>) => void;
addSink: (sink: Omit<Sink, 'id'>) => void;
```

From nes-topology-editor/src/App.tsx:
```typescript
// Currently imports and renders <Palette />
import Palette from './components/Palette/Palette';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Palette and update App layout</name>
  <files>nes-topology-editor/src/App.tsx, nes-topology-editor/src/App.css</files>
  <action>
1. In App.tsx: Remove the `import Palette from './components/Palette/Palette'` line. Remove `<Palette />` from the JSX. The layout should just be `<Toolbar />` and `<Canvas />` inside a single column container (no sidebar).

2. In App.css: Remove all palette-related CSS classes: `.palette`, `.palette__title`, `.palette-item`, `.palette-item:hover`, `.palette-item--worker`, `.palette-item--source`, `.palette-item--sink`. The `.app-container` should no longer need `flex-direction: row` since there's no sidebar -- simplify to just a column layout that fills the viewport.

3. Do NOT delete the Palette.tsx file itself (cleanup can happen later), just remove all references from App.tsx and App.css.

4. In Canvas.tsx: Remove the `onDragOver` and `onDrop` handlers and their corresponding props on `<ReactFlow>`. These were only used for palette drag-and-drop. Remove the `addPhysicalSource`, `addSink` selector lines that were only used in `onDrop` (but keep them if they're used elsewhere -- check first). Remove the `DragEvent` import if no longer needed.
  </action>
  <verify>
    <automated>cd /IdeaProjects/nebulastream2/nes-topology-editor && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>App renders without Palette sidebar; no palette imports remain in App.tsx; palette CSS removed from App.css; Canvas has no DnD handlers</done>
</task>

<task type="auto">
  <name>Task 2: Add Source/Sink buttons to Toolbar and update tests</name>
  <files>nes-topology-editor/src/components/Toolbar/Toolbar.tsx, nes-topology-editor/src/__tests__/palette-dnd.test.tsx</files>
  <action>
1. In Toolbar.tsx: Add two new buttons after the existing "Add Worker" button:
   - "Add Source" button: Uses `Database` icon from lucide-react. onClick calls `addPhysicalSource` with defaults: `{ logicalSourceId: '', hostWorkerId: '', type: 'Generator', sourceConfig: {}, parserConfig: {}, position }` where position is computed via `screenToFlowPosition({ x: window.innerWidth / 2, y: window.innerHeight / 2 })`. Add `data-testid="add-source-btn"`.
   - "Add Sink" button: Uses `FileOutput` icon from lucide-react. onClick calls `addSink` with defaults: `{ name: 'New Sink', hostWorkerId: '', type: 'Print', schema: [], config: {}, position }` using same position computation. Add `data-testid="add-sink-btn"`.
   - Wire `addPhysicalSource` and `addSink` from the store using `useStore((s) => s.addPhysicalSource)` and `useStore((s) => s.addSink)`.

2. Rewrite `palette-dnd.test.tsx` (rename conceptually to test toolbar node creation instead of palette DnD):
   - Remove all Palette-related test cases (the "Palette" describe block).
   - Keep the Canvas import and mock setup.
   - Replace the "Canvas drop handler" tests with tests that verify toolbar buttons exist and call the correct store actions:
     - Test: render Toolbar, find button with `data-testid="add-source-btn"`, click it, verify `addPhysicalSource` was called.
     - Test: render Toolbar, find button with `data-testid="add-sink-btn"`, click it, verify `addSink` was called.
     - Test: verify all three buttons (worker, source, sink) exist in toolbar.
   - The mock setup needs to include `addPhysicalSource` and `addSink` in the store mock, plus `useReactFlow` mock for `screenToFlowPosition` and `fitView`.
  </action>
  <verify>
    <automated>cd /IdeaProjects/nebulastream2/nes-topology-editor && npx vitest run src/__tests__/palette-dnd.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Toolbar shows Add Worker, Add Source, and Add Sink buttons; clicking each creates the corresponding node; tests pass</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` has no errors related to Palette imports
- `npx vitest run --reporter=verbose` -- all tests pass
- No references to Palette in App.tsx
- Toolbar has 3 node creation buttons (worker, source, sink)
</verification>

<success_criteria>
- Palette sidebar is gone from the rendered UI
- Toolbar provides buttons for creating all 3 node types
- All existing tests pass; palette DnD tests replaced with toolbar button tests
- CANV-01/CANV-02 coverage maintained via toolbar buttons
</success_criteria>

<output>
After completion, create `.planning/phases/01-interactive-canvas/01-06-SUMMARY.md`
</output>
