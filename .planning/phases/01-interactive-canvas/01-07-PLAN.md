---
phase: 01-interactive-canvas
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - nes-topology-editor/src/store/selectors.ts
  - nes-topology-editor/src/styles/nodes.css
  - nes-topology-editor/src/components/Canvas/Canvas.tsx
  - nes-topology-editor/src/__tests__/delete.test.tsx
autonomous: true
gap_closure: true
requirements: [CANV-06]

must_haves:
  truths:
    - "Edges display directional arrowheads showing flow direction"
    - "Node deletion has a visible UI affordance beyond keyboard shortcuts"
    - "Connection handles appear at correct visual positions per node type"
    - "TypeScript type errors in delete.test.tsx are resolved"
  artifacts:
    - path: "nes-topology-editor/src/store/selectors.ts"
      provides: "Edges with MarkerType.ArrowClosed on markerEnd"
      contains: "MarkerType"
    - path: "nes-topology-editor/src/components/Canvas/Canvas.tsx"
      provides: "Context menu or delete button for node deletion"
      contains: "delete"
  key_links:
    - from: "nes-topology-editor/src/store/selectors.ts"
      to: "@xyflow/react MarkerType"
      via: "import and edge markerEnd property"
      pattern: "markerEnd.*MarkerType"
---

<objective>
Fix four visual/UX gaps from verification: add directional arrows on edges, improve node deletion discoverability, verify handle CSS positioning, and fix TypeScript errors in delete tests.

Purpose: These are UX polish gaps identified during human verification. Edges need arrows to show flow direction, deletion needs a visible affordance, handles may need CSS fixes, and test type errors should be cleaned up.
Output: Arrows on edges, delete button visible on selection, handles correctly positioned, clean TypeScript compilation.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-interactive-canvas/01-VERIFICATION.md

<interfaces>
From nes-topology-editor/src/store/selectors.ts:
```typescript
// Current edge creation -- no markerEnd property
edges.push({
  id: `downstream-${worker.id}-${targetId}`,
  source: worker.id,
  target: targetId,
  type: 'default',
  label: 'downstream',
});
```

From @xyflow/react:
```typescript
import { MarkerType } from '@xyflow/react';
// MarkerType.ArrowClosed provides a filled arrowhead on edge end
```

From nes-topology-editor/src/components/Canvas/Canvas.tsx:
```typescript
// Current deletion: only via deleteKeyCode={['Backspace', 'Delete']} prop
// onDelete handler already exists and dispatches to store
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edge arrows and fix handle CSS</name>
  <files>nes-topology-editor/src/store/selectors.ts, nes-topology-editor/src/styles/nodes.css</files>
  <action>
1. In selectors.ts: Import `MarkerType` from `@xyflow/react`. Add `markerEnd: { type: MarkerType.ArrowClosed }` to ALL edge objects in `selectReactFlowEdges`:
   - Downstream edges (worker-to-worker): Add markerEnd with ArrowClosed
   - Source-to-worker placement edges: Add markerEnd with ArrowClosed
   - Sink-to-worker placement edges: Add markerEnd with ArrowClosed

2. In nodes.css: Add explicit handle positioning CSS to ensure React Flow handles render at their declared positions. React Flow handles use absolute positioning within the node. Add these rules:

```css
/* Ensure handles render at correct positions */
.worker-node {
  position: relative;
}

.source-node {
  position: relative;
}

.sink-node {
  position: relative;
}
```

The React Flow Handle component uses Position enum to set CSS (top/bottom/left/right). If handles still appear at wrong positions after this, the issue is likely the node container needing `position: relative`. The existing node CSS already uses `display: flex` which should be fine -- just ensure `position: relative` is set so absolute-positioned handles anchor correctly.
  </action>
  <verify>
    <automated>cd /IdeaProjects/nebulastream2/nes-topology-editor && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All edges in selectors.ts have markerEnd with ArrowClosed; node CSS has position:relative for handle anchoring</done>
</task>

<task type="auto">
  <name>Task 2: Add visible delete affordance and fix test type errors</name>
  <files>nes-topology-editor/src/components/Canvas/Canvas.tsx, nes-topology-editor/src/__tests__/delete.test.tsx</files>
  <action>
1. In Canvas.tsx: Add a floating "Delete Selected" toolbar button that appears when nodes or edges are selected. Implementation:
   - Track selection state: use the existing `rfNodes` state to check if any node has `selected: true`. Create a derived `hasSelection` variable: `const hasSelection = rfNodes.some(n => n.selected) || rfEdges.some(e => e.selected);`
   - Add a "Delete Selected" button that appears conditionally when `hasSelection` is true. Position it as a floating bar above the canvas (absolute positioned, top-right or top-center of canvas-container).
   - The button onClick should: collect all selected nodes and edges from `rfNodes`/`rfEdges`, then call the existing `onDelete` handler with them. Use the `Trash2` icon from lucide-react.
   - Style: Use inline styles or a CSS class. Background white, border, rounded, shadow, with the Trash2 icon and "Delete" text. Add `data-testid="delete-selected-btn"`.
   - Import `Trash2` from `lucide-react`.

2. In delete.test.tsx: Fix the 3 TypeScript type errors on `testEdges[0]`, `testEdges[1]`, `testEdges[2]` array access. The issue is that `Array.prototype[number]` returns `T | undefined` in strict mode. Fix by adding non-null assertions (`testEdges[0]!`) or by using intermediate variables with type guards. Non-null assertions are fine here since we control the test data and know the indices exist. Apply `!` to lines 128, 136, and 144 (the `testEdges[N]` accesses).
  </action>
  <verify>
    <automated>cd /IdeaProjects/nebulastream2/nes-topology-editor && npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/__tests__/delete.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>"Delete Selected" button appears when nodes/edges are selected; clicking it removes them; TypeScript compiles cleanly; all delete tests pass</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` reports 0 errors
- `npx vitest run --reporter=verbose` -- all tests pass
- Edges render with arrowheads (visual check in dev server)
- Selecting a node shows a visible "Delete" button
- Handles appear at correct positions per node type
</verification>

<success_criteria>
- All edges have directional arrowheads (markerEnd with ArrowClosed)
- Selected nodes/edges show a visible "Delete Selected" button
- Node handle CSS properly supports React Flow Position enum rendering
- Zero TypeScript type errors in delete.test.tsx
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-interactive-canvas/01-07-SUMMARY.md`
</output>
