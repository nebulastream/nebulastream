---
phase: 01-interactive-canvas
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified: []
autonomous: false
requirements:
  - CANV-01
  - CANV-02
  - CANV-03
  - CANV-04
  - CANV-05
  - CANV-06
  - CANV-07
  - CANV-08
  - CANV-09
  - CANV-10
  - CANV-11
  - CANV-12

must_haves:
  truths:
    - "All 12 CANV requirements are visually verified by the user"
    - "The canvas feels responsive and interactions are intuitive"
  artifacts: []
  key_links: []
---

<objective>
Human verification that the complete interactive canvas works as expected.

Purpose: Automated tests cover logic, but visual UX (node appearance, drag feel, layout aesthetics, minimap usability) requires human eyes. This checkpoint ensures the canvas meets the user's vision before moving to Phase 2.
Output: User approval of the interactive canvas, confirming Phase 1 is complete.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-interactive-canvas/01-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of complete interactive canvas</name>
  <files>nes-topology-editor/src</files>
  <action>
    Start the dev server with `cd nes-topology-editor && npm run dev` and open http://localhost:5173 in the browser.

    What was built:
    - Three visually distinct node types (workers large/blue, sources small/color-coded, sinks small/amber)
    - Palette sidebar with draggable node templates
    - "+" toolbar button for creating workers
    - Worker-to-worker connections (downstream links) via handle dragging
    - Source/sink-to-worker attachment via edges
    - DAG cycle prevention with user feedback
    - Connection type validation (prevents invalid pairings)
    - Select and delete nodes/edges (Backspace/Delete)
    - Grid snapping (20px)
    - Minimap thumbnail overview
    - Auto-layout (dagre hierarchical)
    - Copy/paste with Ctrl+C/Ctrl+V
    - Drag-to-reassign sources/sinks between workers

    How to verify each CANV requirement:
    1. CANV-01, CANV-02: Drag "Worker", "Source", "Sink" from the left palette onto the canvas
    2. CANV-03: Drag nodes around on the canvas to reposition
    3. CANV-04: Drag from a worker bottom handle to another worker top handle to create downstream link
    4. CANV-05: Drag a source node onto a worker to attach it via edge
    5. CANV-06: Select a node or edge, press Backspace or Delete to remove
    6. CANV-07: Workers are large with host label, sources are small and color-coded, sinks are small with uniform color
    7. CANV-08: Try connecting two source nodes (prevented). Create A->B->C, try C->A (prevented with message)
    8. CANV-09: Nodes snap to 20px grid when repositioned
    9. CANV-10: Minimap visible showing thumbnail of full graph
    10. CANV-11: Click "Auto Layout" button in toolbar for hierarchical arrangement
    11. CANV-12: Select nodes, Ctrl+C, Ctrl+V to copy/paste with offset
  </action>
  <verify>User types "approved" or describes issues to fix.</verify>
  <done>User approves the interactive canvas, confirming all 12 CANV requirements work correctly in the browser.</done>
</task>

</tasks>

<verification>
User confirms all 12 CANV requirements work correctly in the browser.
</verification>

<success_criteria>
- User approves the interactive canvas
- All CANV-01 through CANV-12 verified visually
- No blocking UX issues identified
</success_criteria>

<output>
After completion, create `.planning/phases/01-interactive-canvas/01-05-SUMMARY.md`
</output>
