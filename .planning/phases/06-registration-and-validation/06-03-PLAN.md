---
phase: 06-registration-and-validation
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - nes-systests/sources/TokioFileSink.test
autonomous: false
requirements: [VAL-02, VAL-03]
must_haves:
  truths:
    - "A system test runs TokioSource (generator) through a pipeline into AsyncFileSink"
    - "The systest uses CREATE SINK ... TYPE TokioFileSink syntax"
    - "Systest infrastructure can exercise TokioFileSink as a sink type"
  artifacts:
    - path: "nes-systests/sources/TokioFileSink.test"
      provides: "End-to-end systest for TokioFileSink"
      contains: "TokioFileSink"
  key_links:
    - from: "nes-systests/sources/TokioFileSink.test"
      to: "nes-sinks/src/TokioFileSink.cpp"
      via: "CREATE SINK TYPE TokioFileSink triggers TokioSinkRegistry lookup"
      pattern: "TYPE TokioFileSink"
---

<objective>
Create end-to-end system test for TokioFileSink and verify the full source-to-sink pipeline works.

Purpose: Validates the entire v1.1 sink pipeline end-to-end: TokioGenerator sources emit tuples, pipeline routes them through TokioSink to AsyncFileSink via Rust FFI (VAL-02, VAL-03).
Output: TokioFileSink.test systest file, verified end-to-end execution.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-registration-and-validation/06-CONTEXT.md
@.planning/phases/06-registration-and-validation/06-RESEARCH.md
@.planning/phases/06-registration-and-validation/06-01-SUMMARY.md
@.planning/phases/06-registration-and-validation/06-02-SUMMARY.md

<interfaces>
<!-- Systest file format from existing TokioSource.test -->

From nes-systests/sources/TokioSource.test (first 10 + last 10 lines):
```
# name: sources/TokioSource.test
# description: Tests TokioGenerator -- 10000 physical sources x 1000 tuples
# groups: [Sources, TokioSource]

CREATE LOGICAL SOURCE tokioGen(value UINT64 NOT NULL);
CREATE PHYSICAL SOURCE FOR tokioGen TYPE TokioGenerator SET(
    1000 AS `SOURCE`.GENERATOR_COUNT,
    0 AS `SOURCE`.GENERATOR_INTERVAL_MS
);
... (10000 physical sources)

CREATE SINK tokio_checksum(tokioGen.value UINT64 NOT NULL) TYPE Checksum;
SELECT * FROM tokioGen INTO tokio_checksum;
----
10000000 0
```

From 06-CONTEXT.md decisions:
- Scale matches TokioSource systest: 10000 sources x 1000 tuples = 10M tuples
- Sink type name: TokioFileSink
- Uses CREATE SINK ... TYPE TokioFileSink syntax
- No special verification logic -- let systest infra handle pass/fail
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TokioFileSink systest and verify build</name>
  <files>
    nes-systests/sources/TokioFileSink.test
  </files>
  <read_first>
    nes-systests/sources/TokioSource.test,
    nes-sinks/src/TokioFileSink.cpp,
    nes-sinks/src/SinkProvider.cpp
  </read_first>
  <action>
1. Read the full TokioSource.test to understand the exact format (10000 CREATE PHYSICAL SOURCE blocks, final query + expected output section).

2. Create `nes-systests/sources/TokioFileSink.test` with:
   - Header comments: name, description, groups
   - Same 10000 TokioGenerator physical sources as TokioSource.test (copy the CREATE PHYSICAL SOURCE blocks)
   - A TokioFileSink sink with FILE_PATH config:
     ```
     CREATE SINK fileSink(value UINT64 NOT NULL) TYPE TokioFileSink SET(
         '/tmp/nes-tokio-file-sink-test.bin' AS `SINK`.FILE_PATH
     );
     ```
   - Query routing data to the sink:
     ```
     SELECT * FROM tokioGen INTO fileSink;
     ```
   - For the verification section after `----`: Use the same format as TokioSource.test (which uses a Checksum sink with expected output). Since TokioFileSink writes raw binary and has no text output to match, omit the `----` verification section entirely. Per user decision: "let systest infra handle pass/fail, inspect manually if needed." The test passes if the pipeline completes without error (no crash, no timeout). If the systest framework requires a `----` section, check how TokioSource.test structures its verification and add a minimal placeholder that the infra accepts.

3. Verify the full project builds:
   ```
   cmake --build cmake-build-debug -j$(nproc)
   ```

4. Run existing TokioSink and TokioSource tests to ensure no regressions:
   ```
   ctest --test-dir cmake-build-debug -R "TokioSink|TokioSource" -j --output-on-failure
   ```
  </action>
  <verify>
    <automated>cd /tmp/tokio-sources && cmake --build cmake-build-debug -j$(nproc) 2>&1 | tail -10 && ctest --test-dir cmake-build-debug -R "TokioSink" -j --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f nes-systests/sources/TokioFileSink.test (file exists)
    - grep -q "TokioFileSink" nes-systests/sources/TokioFileSink.test
    - grep -q "CREATE SINK" nes-systests/sources/TokioFileSink.test
    - grep -q "FILE_PATH" nes-systests/sources/TokioFileSink.test
    - cmake --build cmake-build-debug exits 0 (full build passes)
    - ctest -R TokioSink shows existing tests pass
  </acceptance_criteria>
  <done>TokioFileSink.test systest file exists with 10000 sources, TokioFileSink sink with FILE_PATH config. Full build passes. Existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 6 pipeline</name>
  <files>nes-systests/sources/TokioFileSink.test</files>
  <action>
Present the complete Phase 6 implementation to the user for verification. The full pipeline is:
- Rust: SinkMessage::Flush, AsyncFileSink (tokio::fs raw binary), spawn_file_sink FFI
- C++: TokioSinkRegistry, TokioFileSink factory, SinkProvider routing, WorkerConfiguration
- Systest: TokioFileSink.test with 10000 sources x 1000 tuples

User should verify:
1. Rust tests pass: `cd nes-sources/rust && cargo test -p nes_source_runtime --lib`
2. C++ build succeeds: `cmake --build cmake-build-debug -j$(nproc)`
3. TokioSink tests pass: `ctest --test-dir cmake-build-debug -R TokioSink -j --output-on-failure`
4. (Optional) Run TokioFileSink systest: `ctest --test-dir cmake-build-debug -R TokioFileSink --output-on-failure`
5. Registry contains TokioFileSink (check build output for plugin registration)
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <done>User has approved the complete Phase 6 implementation or provided feedback for fixes.</done>
</task>

</tasks>

<verification>
Full pipeline verified:
1. Rust tests: `cargo test -p nes_source_runtime --lib` -- all pass
2. C++ build: `cmake --build cmake-build-debug -j` -- succeeds
3. C++ tests: `ctest --test-dir cmake-build-debug -R TokioSink -j` -- pass
4. Systest file exists with correct structure
</verification>

<success_criteria>
- TokioFileSink.test systest exists with 10000 sources x 1000 tuples scale
- Full project builds successfully
- All existing TokioSink and TokioSource tests pass (no regressions)
- Human verified the end-to-end pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/06-registration-and-validation/06-03-SUMMARY.md`
</output>
