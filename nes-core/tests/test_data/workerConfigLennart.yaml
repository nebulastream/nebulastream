# ~~~ Configurations for the NES Worker ~~~

# ~~ IPs and Ports ~~
# Set worker IP
localWorkerIp: localhost

# Set RPC Server port of NES Coordinator to which the NES Worker should connect
# Needs to be the same as rpcPort in Coordinator
coordinatorPort: 5000

# Set the number of worker threads
numWorkerThreads: 5

# Number buffers in global buffer pool
numberOfBuffersInGlobalBufferManager: 2048

# Number buffers in source local buffer pool
numberOfBuffersInSourceLocalBufferPool: 128

 Monitoring Configuration
#monitoringConfiguration: '
#  - cpu:
#    attributes: "irq, user, system"
#    sampleRate: 60
#'
monitoringConfiguration: '
  - cpu:
    attributes: "coreNum, user, system"
    sampleRate: 6000
  - disk:
    attributes: "F_BSIZE, F_BLOCKS, F_FRSIZE"
    sampleRate: 5000
  - memory:
    attributes: "FREE_RAM, FREE_SWAP, TOTAL_RAM"
    sampleRate: 4000
  - network:
    attributes: "rBytes, rFifo, tPackets"
    sampleRate: 3000
'

#monitoringConfiguration: '
#  - disk:
#    attributes: "F_BSIZE, F_BLOCKS, F_FRSIZE"
#    sampleRate: 50
#'

#monitoringConfiguration:
#  - CPUMetric:
#      metrics: 'coreNum, user, system'
#      sampleRate: 60   # Samplerate in Sekunden
#  - DiskMetric:
#      metrics: 'F_FRSIZE, F_BLOCKS, F_BFREE'
#      sampleRate: 60   # Samplerate in Sekunden


