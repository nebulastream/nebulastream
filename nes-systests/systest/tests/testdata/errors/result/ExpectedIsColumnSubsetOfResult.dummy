Source stream UINT64 id UINT64 value INLINE
1,1

SINK CORRECT_SINK UINT64 stream$id UINT64 stream$otherId
SELECT id, id AS otherId FROM stream INTO CORRECT_SINK
----
1 1

SINK INCORRECT_SINK UINT64 stream$id
SELECT id, id AS otherId FROM stream INTO INCORRECT_SINK
----
1
