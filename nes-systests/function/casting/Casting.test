# name: projection/Projection.test
# description: Simple map tests with an add function
# groups: [Projection]

CREATE LOGICAL SOURCE stream(id UINT32 NOT NULL, value UINT32 NOT NULL, timestamp UINT32 NOT NULL);
CREATE PHYSICAL SOURCE FOR stream TYPE File;
ATTACH INLINE
1,1,1000
2,1,1001
3,1,1002
4,2,2000
5,19,19000
6,20,20000
7,21,21000

CREATE SINK sinkId(u64id UINT64 NOT NULL) TYPE File;

# 1: [id]
SELECT UINT64(id) as u64id FROM stream INTO sinkId;
----
1
2
3
4
5
6
7

# 2: [id + const]
SELECT UINT64(id + UINT64(1)) as u64id FROM stream INTO sinkId;
----
2
3
4
5
6
7
8

# 3: [id + const]
SELECT UINT64(id) + UINT64(1) as u64id FROM stream INTO sinkId;
----
2
3
4
5
6
7
8

# 4: [id + value]
SELECT UINT64(id + value) as u64id FROM stream INTO sinkId;
----
2
3
4
6
24
26
28