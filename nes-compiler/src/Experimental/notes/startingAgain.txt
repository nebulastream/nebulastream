Status:
- a lot of functionality is static (assumes types etc.)
    - TODO: remove all static assumptions
- messily indexing in NESAbstractionToMLIR.cpp
    - TODO: establish proper indexing
    - TODO: iterate over TB, instead of static 42 times
- we only print content of TB
    - TODO: write results to result TB
- we call insertBlock->push_back(..) everywhere
    - TODO: find out whether to avoid this by using 'builder->setInsertionPointToStart()'
        - look at the createGlobalString() function in NESAbstractionToMLIR.cpp