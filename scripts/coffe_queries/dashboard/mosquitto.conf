# Coffee demo broker.
#   1883 native MQTT  -> NES MQTT sinks publish here (worker, over the docker network)
#   9001 websockets   -> Firefox / the dashboard connect here (ws://<host>:9001/mqtt)
listener 1883
allow_anonymous true

listener 9001
protocol websockets
allow_anonymous true
