[package]
name = "nes_buffer_runtime"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
nes_buffer_bindings.workspace = true
cxx = "1.0"
tokio = { version = "1.40.0", features = ["sync"] }
