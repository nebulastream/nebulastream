diff --git a/CMakeLists.txt b/CMakeLists.txt
index c07add6..07579f7 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -5,6 +5,7 @@ project(
   HOMEPAGE_URL "https://github.com/exaloop/codon"
   DESCRIPTION "high-performance, extensible Python compiler")
 set(CODON_JIT_PYTHON_VERSION "0.4.6")
+option(CODON_COMPILER_ONLY "Build only the AOT compiler library" OFF)
 configure_file("${PROJECT_SOURCE_DIR}/cmake/config.h.in"
                "${PROJECT_SOURCE_DIR}/codon/config/config.h")
 configure_file("${PROJECT_SOURCE_DIR}/cmake/config.py.in"
@@ -53,7 +56,7 @@ endif()
 
 add_executable(peg2cpp codon/util/peg2cpp.cpp)
 target_include_directories(peg2cpp PRIVATE ${peglib_SOURCE_DIR})
-target_link_libraries(peg2cpp PRIVATE Threads::Threads fmt)
+target_link_libraries(peg2cpp PRIVATE Threads::Threads fmt::fmt)
 add_custom_command(
   OUTPUT codon_rules.cpp
   COMMAND peg2cpp ${CMAKE_SOURCE_DIR}/codon/parser/peg/grammar.peg
@@ -65,7 +68,8 @@ add_custom_command(
           omp
   DEPENDS peg2cpp codon/parser/peg/openmp.peg)
 
-# Codon Jupyter library
+# Codon Jupyter library and runtime
+if(NOT CODON_COMPILER_ONLY)
 set(CODON_JUPYTER_FILES codon/util/jupyter.h codon/util/jupyter.cpp)
 add_library(codon_jupyter SHARED ${CODON_JUPYTER_FILES})
 
@@ -170,6 +174,7 @@ add_custom_command(
   POST_BUILD
   COMMAND ${CMAKE_COMMAND} -E copy_if_different $<TARGET_FILE:omp>
           ${CMAKE_BINARY_DIR})
+endif()
 
 # Codon compiler library
 include_directories(${LLVM_INCLUDE_DIRS})
@@ -382,11 +387,33 @@ set(CODON_CPPFILES
   codon/cir/transform/numpy/indexing.cpp
   codon/cir/transform/numpy/numpy.cpp
   codon/util/common.cpp)
-add_library(codonc SHARED ${CODON_HPPFILES})
-target_include_directories(codonc PRIVATE ${peglib_SOURCE_DIR}
-                                          ${toml_SOURCE_DIR}/include
-                                          ${semver_SOURCE_DIR}/include
-                                          ${fast_float_SOURCE_DIR}/include)
+if(CODON_COMPILER_ONLY)
+  list(REMOVE_ITEM CODON_CPPFILES
+    codon/compiler/debug_listener.cpp
+    codon/compiler/engine.cpp
+    codon/compiler/jit.cpp
+    codon/compiler/memory_manager.cpp
+    codon/dsl/plugins.cpp
+    codon/cir/llvm/gpu.cpp)
+endif()
+if(CODON_COMPILER_ONLY)
+  add_library(codonc STATIC ${CODON_HPPFILES})
+  set_property(TARGET codonc PROPERTY EXPORT_NAME compiler)
+else()
+  add_library(codonc SHARED ${CODON_HPPFILES})
+endif()
+target_include_directories(codonc PUBLIC
+  $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
+  $<INSTALL_INTERFACE:include>)
+target_include_directories(codonc PRIVATE ${peglib_SOURCE_DIR} ${fast_float_INCLUDE_DIR})
+if(NOT CODON_COMPILER_ONLY)
+  target_include_directories(codonc PRIVATE ${toml_SOURCE_DIR}/include
+                                            ${semver_SOURCE_DIR}/include
+                                            ${fast_float_SOURCE_DIR}/include)
+else()
+  target_compile_definitions(codonc PUBLIC CODON_COMPILER_ONLY)
+  target_compile_options(codonc PRIVATE -fno-rtti)
+endif()
 target_sources(codonc PRIVATE ${CODON_CPPFILES} codon_rules.cpp omp_rules.cpp)
 if(ASAN)
   target_compile_options(
@@ -434,13 +461,24 @@ file(GLOB_RECURSE CODON_STDLIB_RESOURCES
   CONFIGURE_DEPENDS
   "${CMAKE_CURRENT_SOURCE_DIR}/stdlib/*.codon"
 )
+set(CODON_STDLIB_TYPE)
+if(CODON_COMPILER_ONLY)
+  list(APPEND CODON_STDLIB_TYPE TYPE OBJECT)
+endif()
 cmrc_add_resource_library(
   codon-stdlib
   NAMESPACE codon
+  ${CODON_STDLIB_TYPE}
   ${CODON_STDLIB_RESOURCES}
 )
 set_property(TARGET codon-stdlib PROPERTY POSITION_INDEPENDENT_CODE ON)
 
-target_link_libraries(codonc PRIVATE ${LLVM_LIBS} fmt dl codonrt codon-stdlib)
+target_link_libraries(codonc PRIVATE ${LLVM_LIBS} fmt::fmt)
+if(CODON_COMPILER_ONLY)
+  target_sources(codonc PRIVATE $<TARGET_OBJECTS:codon-stdlib>)
+  target_include_directories(codonc PRIVATE ${CMRC_INCLUDE_DIR})
+else()
+  target_link_libraries(codonc PRIVATE dl codonrt codon-stdlib)
+endif()
 
 # Gather headers
@@ -454,9 +486,13 @@ add_custom_target(
           "${CMAKE_BINARY_DIR}/include/codon"
   COMMAND find "${CMAKE_BINARY_DIR}/include" -type f ! -name "*.h" -exec rm {}
           \\;)
-add_dependencies(headers codonrt codonc)
+add_dependencies(headers codonc)
+if(NOT CODON_COMPILER_ONLY)
+  add_dependencies(headers codonrt)
+endif()
 
 # Prepare lib directory for plugin compilation
+if(NOT CODON_COMPILER_ONLY)
 add_custom_target(
   libs ALL
   COMMENT "Collecting libraries"
@@ -482,8 +518,10 @@ add_custom_target(
   COMMAND
     ${CMAKE_COMMAND} -E copy ${copied_libgcc} "${CMAKE_BINARY_DIR}/lib/codon")
 add_dependencies(libs codonrt codonc)
+endif()
 
 # Codon command-line tool
+if(NOT CODON_COMPILER_ONLY)
 add_executable(codon codon/app/main.cpp)
 target_link_libraries(codon PUBLIC fmt codonc codon_jupyter Threads::Threads)
 
@@ -519,7 +557,18 @@ target_include_directories(codon_test PRIVATE test/cir "${gc_SOURCE_DIR}/include
 target_link_libraries(codon_test fmt codonc codonrt gtest_main)
 target_compile_definitions(codon_test
                            PRIVATE TEST_DIR="${CMAKE_CURRENT_SOURCE_DIR}/test")
+endif()
 
+if(CODON_COMPILER_ONLY)
+install(TARGETS codonc
+        EXPORT codon-compiler-targets
+        ARCHIVE DESTINATION lib
+        LIBRARY DESTINATION lib)
+install(EXPORT codon-compiler-targets
+        FILE codon-compiler-targets.cmake
+        NAMESPACE codon::
+        DESTINATION share/codon-compiler)
+else()
 install(TARGETS codonrt codonc codon_jupyter DESTINATION lib/codon)
 install(FILES ${CMAKE_BINARY_DIR}/libomp${CMAKE_SHARED_LIBRARY_SUFFIX} DESTINATION lib/codon)
 install(FILES ${copied_libgfortran} DESTINATION lib/codon)
@@ -532,10 +581,17 @@ install(CODE "
 ")
 install(FILES ${copied_libgcc} DESTINATION lib/codon)
 install(TARGETS codon DESTINATION bin)
+endif()
+if(CODON_COMPILER_ONLY)
+install(DIRECTORY ${CMAKE_SOURCE_DIR}/codon DESTINATION include
+        FILES_MATCHING PATTERN "*.h")
+else()
 install(DIRECTORY ${CMAKE_BINARY_DIR}/include/codon DESTINATION include)
+endif()
+if(NOT CODON_COMPILER_ONLY)
 install(DIRECTORY ${LLVM_INCLUDE_DIRS}/llvm DESTINATION include)
 install(DIRECTORY ${LLVM_INCLUDE_DIRS}/llvm-c DESTINATION include)
 install(DIRECTORY ${CMAKE_SOURCE_DIR}/stdlib DESTINATION lib/codon)
 install(DIRECTORY ${CMAKE_SOURCE_DIR}/jit/ DESTINATION python)
 install(DIRECTORY DESTINATION lib/codon/plugins)
 
@@ -553,3 +609,4 @@ install(CODE [[
     endif()
   endif()
 ]])
+endif()
diff --git a/cmake/deps.cmake b/cmake/deps.cmake
index 9717ba8..f79547d 100644
--- a/cmake/deps.cmake
+++ b/cmake/deps.cmake
@@ -6,6 +6,13 @@ if(NOT (EXISTS ${CPM_DOWNLOAD_LOCATION}))
 endif()
 include(${CPM_DOWNLOAD_LOCATION})
 
+if(CODON_COMPILER_ONLY)
+    find_package(fmt CONFIG REQUIRED)
+    find_path(peglib_SOURCE_DIR NAMES peglib.h REQUIRED)
+    find_path(fast_float_INCLUDE_DIR NAMES fast_float/fast_float.h REQUIRED)
+    return()
+endif()
+
 CPMAddPackage(
     NAME peglib
     GITHUB_REPOSITORY "exaloop/cpp-peglib"
diff --git a/codon/cir/llvm/llvisitor.cpp b/codon/cir/llvm/llvisitor.cpp
index 189f59e..7feeeb5 100644
--- a/codon/cir/llvm/llvisitor.cpp
+++ b/codon/cir/llvm/llvisitor.cpp
@@ -13,8 +13,10 @@
 #include "codon/cir/dsl/codegen.h"
 #include "codon/cir/llvm/optimize.h"
 #include "codon/cir/util/irtools.h"
+#ifndef CODON_COMPILER_ONLY
 #include "codon/compiler/debug_listener.h"
 #include "codon/compiler/memory_manager.h"
+#endif
 #include "codon/parser/common.h"
 #include "codon/runtime/lib.h"
 #include "codon/util/common.h"
@@ -107,7 +109,6 @@ LLVMVisitor::LLVMVisitor()
   llvm::initializeTarget(registry);
 
   llvm::initializeExpandLargeDivRemLegacyPassPass(registry);
-  llvm::initializeExpandLargeFpConvertLegacyPassPass(registry);
   llvm::initializeExpandMemCmpLegacyPassPass(registry);
   llvm::initializeScalarizeMaskedMemIntrinLegacyPassPass(registry);
   llvm::initializeSelectOptimizePass(registry);
@@ -256,7 +257,7 @@ std::unique_ptr<llvm::Module> LLVMVisitor::makeModule(llvm::LLVMContext &context
 
   auto target = builder.selectTarget();
   auto M = std::make_unique<llvm::Module>("codon", context);
-  M->setTargetTriple(target->getTargetTriple().str());
+  M->setTargetTriple(target->getTargetTriple());
   M->setDataLayout(target->createDataLayout());
   B = std::make_unique<llvm::IRBuilder<>>(context);
 
@@ -651,8 +652,9 @@ llvm::Function *LLVMVisitor::createPyTryCatchWrapper(llvm::Function *func) {
   auto *unwindExceptionClass = B->CreateLoad(
       B->getInt64Ty(), B->CreateStructGEP(unwindType, unwindException, 0));
   unwindException = B->CreateExtractValue(caughtResult, 0);
-  auto *excVal = B->CreateConstGEP1_64(B->getInt8Ty(), unwindException,
-                                       (uint64_t)seq_exc_offset());
+  auto excOffset = B->CreateCall(
+      M->getOrInsertFunction("seq_exc_offset", B->getInt64Ty()));
+  auto *excVal = B->CreateGEP(B->getInt8Ty(), unwindException, excOffset);
   auto *loadedExc = B->CreateLoad(B->getPtrTy(), excVal);
 
   auto *strType = llvm::StructType::get(B->getInt64Ty(), B->getPtrTy());
@@ -1237,6 +1239,9 @@ void LLVMVisitor::compile(const std::string &filename, const std::string &argv0,
 
 void LLVMVisitor::run(const std::vector<std::string> &args,
                       const std::vector<std::string> &libs, const char *const *envp) {
+#ifdef CODON_COMPILER_ONLY
+  compilationError("JIT execution is unavailable in compiler-only builds");
+#else
   runLLVMPipeline();
 
   Timer t1("llvm/jitlink");
@@ -1297,6 +1302,7 @@ void LLVMVisitor::run(const std::vector<std::string> &args,
     fmt::print(stderr, "{}\n", e.getOutput());
     std::abort();
   }
+#endif
 }
 
 #define ALLOC_FAMILY "seq_alloc"
@@ -3014,8 +3020,9 @@ void LLVMVisitor::visit(const TryCatchFlow *x) {
   // reroute Codon exceptions
   B->SetInsertPoint(tc.exceptionRouteBlock);
   unwindException = B->CreateExtractValue(B->CreateLoad(padType, tc.catchStore), 0);
-  auto *excVal = B->CreateConstGEP1_64(B->getInt8Ty(), unwindException,
-                                       (uint64_t)seq_exc_offset());
+  auto excOffset = B->CreateCall(
+      M->getOrInsertFunction("seq_exc_offset", B->getInt64Ty()));
+  auto *excVal = B->CreateGEP(B->getInt8Ty(), unwindException, excOffset);
   auto *loadedExc = B->CreateLoad(B->getPtrTy(), excVal);
 
   // set depth when catch-all entered
diff --git a/codon/cir/llvm/llvm.h b/codon/cir/llvm/llvm.h
index ac2b543..ae14053 100644
--- a/codon/cir/llvm/llvm.h
+++ b/codon/cir/llvm/llvm.h
@@ -24,6 +24,7 @@
 #include "llvm/CodeGen/TargetPassConfig.h"
 #include "llvm/DebugInfo/Symbolize/Symbolize.h"
 #include "llvm/ExecutionEngine/ExecutionEngine.h"
+#ifndef CODON_COMPILER_ONLY
 #include "llvm/ExecutionEngine/GenericValue.h"
 #include "llvm/ExecutionEngine/JITEventListener.h"
 #include "llvm/ExecutionEngine/JITLink/JITLink.h"
@@ -59,6 +60,7 @@
 #include "llvm/ExecutionEngine/Orc/TargetProcess/TargetExecutionUtils.h"
 #include "llvm/ExecutionEngine/RuntimeDyld.h"
 #include "llvm/ExecutionEngine/SectionMemoryManager.h"
+#endif
 #include "llvm/IR/Argument.h"
 #include "llvm/IR/BasicBlock.h"
 #include "llvm/IR/Constants.h"
diff --git a/codon/cir/llvm/optimize.cpp b/codon/cir/llvm/optimize.cpp
index c0224f7..2d11a7c 100644
--- a/codon/cir/llvm/optimize.cpp
+++ b/codon/cir/llvm/optimize.cpp
@@ -1084,7 +1084,9 @@ void optimize(llvm::Module *module, bool debug, bool jit, PluginManager *plugins
   }
   {
     TIME("llvm/gpu");
+#ifndef CODON_COMPILER_ONLY
     applyGPUTransformations(module);
+#endif
   }
   verify(module);
 }
diff --git a/codon/cir/util/matching.cpp b/codon/cir/util/matching.cpp
index a93aed7..8584bd5 100644
--- a/codon/cir/util/matching.cpp
+++ b/codon/cir/util/matching.cpp
@@ -9,7 +9,8 @@
 
 #define VISIT(x)                                                                       \
   void visit(const x *v) override {                                                    \
-    if (matchAny || dynamic_cast<const util::Any *>(v)) {                              \
+    if (matchAny || v->is<util::AnyValue>() || v->is<util::AnyFlow>() ||               \
+        v->is<util::AnyVar>() || v->is<util::AnyFunc>()) {                             \
       result = true;                                                                   \
       matchAny = true;                                                                 \
     } else if (!nodeId) {                                                              \
diff --git a/codon/compiler/compiler.cpp b/codon/compiler/compiler.cpp
index 5e2d005..932c861 100644
--- a/codon/compiler/compiler.cpp
+++ b/codon/compiler/compiler.cpp
@@ -34,7 +34,12 @@ Compiler::Compiler(const std::string &argv0, Compiler::Mode mode,
                    bool pyNumerics, bool pyExtension,
                    const std::shared_ptr<ast::IFilesystem> &fs)
     : argv0(argv0), debug(mode == Mode::DEBUG), pyNumerics(pyNumerics),
-      pyExtension(pyExtension), input(), plm(std::make_unique<PluginManager>(argv0)),
+      pyExtension(pyExtension), input(),
+#ifdef CODON_COMPILER_ONLY
+      plm(),
+#else
+      plm(std::make_unique<PluginManager>(argv0)),
+#endif
       cache(std::make_unique<ast::Cache>(argv0, fs)),
       module(std::make_unique<ir::Module>()),
       pm(std::make_unique<ir::transform::PassManager>(
@@ -50,6 +55,10 @@ Compiler::Compiler(const std::string &argv0, Compiler::Mode mode,
 }
 
 llvm::Error Compiler::load(const std::string &plugin) {
+#ifdef CODON_COMPILER_ONLY
+  return llvm::createStringError(llvm::inconvertibleErrorCode(),
+                                 "Codon plugins are unavailable in compiler-only builds");
+#else
   auto result = plm->load(plugin);
   if (auto err = result.takeError())
     return err;
@@ -69,6 +78,7 @@ llvm::Error Compiler::load(const std::string &plugin) {
   loadedPlugins.insert(plugin);
 
   return llvm::Error::success();
+#endif
 }
 
 /// Checks if a plugin is already loaded.
diff --git a/codon/parser/ast/types/traits.cpp b/codon/parser/ast/types/traits.cpp
index f9e7ab5..ed07ef2 100644
--- a/codon/parser/ast/types/traits.cpp
+++ b/codon/parser/ast/types/traits.cpp
@@ -172,7 +172,7 @@ int CallableTrait::unify(Type *typ, Unification *us) {
       return unify(tl->type.get(), us);
     if (tl->kind == LinkType::Unbound) {
       if (tl->trait) {
-        auto tt = dynamic_cast<CallableTrait *>(tl->trait.get());
+        auto tt = tl->trait->getCallableTrait();
         if (!tt || tt->args.size() != args.size())
           return -1;
         for (int i = 0; i < args.size(); i++)
diff --git a/codon/parser/ast/types/traits.h b/codon/parser/ast/types/traits.h
index 2e3c712..259cc7c 100644
--- a/codon/parser/ast/types/traits.h
+++ b/codon/parser/ast/types/traits.h
@@ -26,6 +26,7 @@ struct CallableTrait : public Trait {
 
 public:
   explicit CallableTrait(Cache *cache, std::vector<TypePtr> args);
+  CallableTrait *getCallableTrait() override { return this; }
   int unify(Type *typ, Unification *undo) override;
   TypePtr generalize(int atLevel) const override;
   TypePtr instantiate(int atLevel, int *unboundCount,
diff --git a/codon/parser/ast/types/type.h b/codon/parser/ast/types/type.h
index 7a9ba87..1613a6b 100644
--- a/codon/parser/ast/types/type.h
+++ b/codon/parser/ast/types/type.h
@@ -26,6 +26,7 @@ struct IntStaticType;
 struct StrStaticType;
 struct BoolStaticType;
 struct UnionType;
+struct CallableTrait;
 
 enum LiteralKind { Runtime, Int, String, Bool };
 
@@ -117,6 +118,7 @@ public:
   virtual StrStaticType *getStrStatic() { return nullptr; }
   virtual BoolStaticType *getBoolStatic() { return nullptr; }
   virtual UnionType *getUnion() { return nullptr; }
+  virtual CallableTrait *getCallableTrait() { return nullptr; }
 
   virtual bool is(const std::string &s);
 
diff --git a/codon/parser/cache.cpp b/codon/parser/cache.cpp
index 822fb99..aaf59d0 100644
--- a/codon/parser/cache.cpp
+++ b/codon/parser/cache.cpp
@@ -247,7 +247,7 @@ Cache::mergeC3(std::vector<std::vector<types::TypePtr>> &seqs) {
           }
         }
       if (!nothead) {
-        cand = std::dynamic_pointer_cast<types::ClassType>(seq[0]);
+        cand = std::static_pointer_cast<types::ClassType>(seq[0]);
         break;
       }
     }
diff --git a/codon/parser/visitors/translate/translate_ctx.cpp b/codon/parser/visitors/translate/translate_ctx.cpp
index 28b36c4..9b8168c 100644
--- a/codon/parser/visitors/translate/translate_ctx.cpp
+++ b/codon/parser/visitors/translate/translate_ctx.cpp
@@ -67,7 +67,7 @@ void TranslateContext::addSeries(codon::ir::SeriesFlow *s) { series.push_back(s)
 void TranslateContext::popSeries() { series.pop_back(); }
 
 codon::ir::Module *TranslateContext::getModule() const {
-  return dynamic_cast<codon::ir::Module *>(bases[0]->getModule());
+  return bases[0]->getModule();
 }
 codon::ir::BodiedFunc *TranslateContext::getBase() const { return bases.back(); }
 codon::ir::SeriesFlow *TranslateContext::getSeries() const { return series.back(); }
diff --git a/codon/cir/llvm/llvisitor.cpp b/codon/cir/llvm/llvisitor.cpp
--- a/codon/cir/llvm/llvisitor.cpp
+++ b/codon/cir/llvm/llvisitor.cpp
@@ -370,6 +370,8 @@ void LLVMVisitor::process(const Node *x) {
 
 void LLVMVisitor::dump(const std::string &filename) { writeToLLFile(filename, false); }
 
+void LLVMVisitor::optimizeLLVM() { runLLVMPipeline(); }
+
 void LLVMVisitor::runLLVMPipeline() {
   db.builder->finalize();
   optimize(M.get(), db.debug, db.jit, plugins);
diff --git a/codon/cir/llvm/llvisitor.h b/codon/cir/llvm/llvisitor.h
--- a/codon/cir/llvm/llvisitor.h
+++ b/codon/cir/llvm/llvisitor.h
@@ -344,6 +344,10 @@ public:
   /// Dumps the unoptimized module IR to a file.
   /// @param filename name of file to write IR to
   void dump(const std::string &filename = "_dump.ll");
+
+  /// Runs Codon's LLVM optimization pipeline without writing the module.
+  void optimizeLLVM();
+
   /// Writes module as native object file.
   /// @param filename the .o file to write to
   /// @param pic true to write position-independent code
diff --git a/codon/cir/llvm/optimize.cpp b/codon/cir/llvm/optimize.cpp
--- a/codon/cir/llvm/optimize.cpp
+++ b/codon/cir/llvm/optimize.cpp
@@ -1072,6 +1072,10 @@ void verify(llvm::Module *module) {
 
 } // namespace
 
+void setNativeOptimizationEnabled(bool enabled) {
+  DisableNative = !enabled;
+}
+
 void optimize(llvm::Module *module, bool debug, bool jit, PluginManager *plugins) {
   verify(module);
   {
diff --git a/codon/cir/llvm/optimize.h b/codon/cir/llvm/optimize.h
--- a/codon/cir/llvm/optimize.h
+++ b/codon/cir/llvm/optimize.h
@@ -18,5 +18,8 @@ getTargetMachine(llvm::Module *module, bool setFunctionAttributes = false,
                  bool pic = false);
 
+/// Enables or disables architecture-specific LLVM optimizations.
+void setNativeOptimizationEnabled(bool enabled);
+
 void optimize(llvm::Module *module, bool debug, bool jit = false,
               PluginManager *plugins = nullptr);
 
diff --git a/codon/parser/common.cpp b/codon/parser/common.cpp
--- a/codon/parser/common.cpp
+++ b/codon/parser/common.cpp
@@ -117,8 +117,9 @@ ResourceFilesystem::ResourceFilesystem(const std::string &argv0,
 
 std::vector<std::string> ResourceFilesystem::read_lines(const path_t &path) const {
   auto fs = cmrc::codon::get_filesystem();
+  const auto normalizedPath = path.lexically_normal();
 
-  if (!fs.exists(path) && allowExternal)
+  if (!fs.exists(normalizedPath) && allowExternal)
     return Filesystem::read_lines(path);
 
   std::vector<std::string> lines;
@@ -126,7 +127,7 @@ std::vector<std::string> ResourceFilesystem::read_lines(const path_t &path) cons
     E(error::Error::COMPILER_NO_FILE, SrcInfo(), "<stdin>");
   } else {
     try {
-      auto fd = fs.open(path);
+      auto fd = fs.open(normalizedPath);
       auto contents = std::string(fd.begin(), fd.end());
       lines = split(contents, '\n');
     } catch (std::system_error &) {
@@ -138,13 +139,20 @@ std::vector<std::string> ResourceFilesystem::read_lines(const path_t &path) cons
 
 bool ResourceFilesystem::exists(const path_t &path) const {
   auto fs = cmrc::codon::get_filesystem();
-  if (fs.exists(path))
+  if (fs.exists(path.lexically_normal()))
     return true;
   if (allowExternal)
     return Filesystem::exists(path);
   return false;
 }
 
+IFilesystem::path_t ResourceFilesystem::canonical(const path_t &path) const {
+  const auto normalizedPath = path.lexically_normal();
+  if (cmrc::codon::get_filesystem().exists(normalizedPath))
+    return normalizedPath;
+  return allowExternal ? Filesystem::canonical(path) : normalizedPath;
+}
+
 /// String and collection utilities
 
 std::vector<std::string> split(const std::string &s, char delim) {
diff --git a/codon/parser/common.h b/codon/parser/common.h
--- a/codon/parser/common.h
+++ b/codon/parser/common.h
@@ -216,6 +216,7 @@ public:
                      bool allowExternal = true);
   std::vector<std::string> read_lines(const path_t &path) const override;
   bool exists(const path_t &path) const override;
+  path_t canonical(const path_t &path) const override;
 };
 
 /// Find an import file what given an executable path (argv0) either in the standard
diff --git a/codon/parser/common.cpp b/codon/parser/common.cpp
--- a/codon/parser/common.cpp
+++ b/codon/parser/common.cpp
@@ -410,6 +410,15 @@ std::shared_ptr<ImportFile> getImportFile(Cache *cache, const std::string &what,
     if (fs->exists(path))
       paths.emplace_back(fs->canonical(path));
 
+    path = p / what;
+    path.replace_extension("py");
+    if (fs->exists(path))
+      paths.emplace_back(fs->canonical(path));
+
+    path = p / what / "__init__.py";
+    if (fs->exists(path))
+      paths.emplace_back(fs->canonical(path));
+
     // Load a plugin maybe
     checkPlugin(p, what);
   }
