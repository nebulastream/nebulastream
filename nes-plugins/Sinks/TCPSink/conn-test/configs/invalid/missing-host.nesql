-- Invalid: `socket_host` is required, so DescriptorConfig::validateAndFormat
-- rejects this at bind time with INVALID_CONFIG_PARAMETER (1000).
CREATE SINK conntest_sink (value VARSIZED NOT NULL) TYPE TCP SET (
    '9000' AS `SINK`.socket_port,
    'JSON' AS `SINK`.OUTPUT_FORMAT);
