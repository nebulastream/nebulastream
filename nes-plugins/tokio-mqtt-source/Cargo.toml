[package]
name = "nes_tokio_mqtt_source"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
nes_mqtt_broker = { path = "../nes-mqtt-broker" }
nes_source_runtime = { path = "../../nes-sources/rust/nes-source-runtime" }
tokio = { version = "1.40.0", features = ["time", "sync"] }
tracing = "0.1"
