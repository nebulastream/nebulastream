[package]
name = "nes_buffer_bindings"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
cxx = "1.0"
tokio = { version = "1.40.0", features = ["rt-multi-thread"] }
tracing = "0.1"
