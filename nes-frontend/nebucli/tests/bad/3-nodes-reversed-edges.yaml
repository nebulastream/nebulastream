# Tests placement failure due to reversed downstream edges.
# node1 hosts the sink but declares downstream toward node3 (the source),
# making operator placement infeasible. Expected: placement fails with
# "topology is not connected".

query: |
  SELECT * FROM GENERATOR_SOURCE INTO VOID_SINK

sinks:
  - name: VOID_SINK
    host: node1:8080
    schema:
      - name: DOUBLE
        type: FLOAT64
    sink_type: Print
    config:
      output_format: CSV
    parser_config: { }

logical_sources:
  - name: GENERATOR_SOURCE
    schema:
      - name: DOUBLE
        type: FLOAT64

physical_sources:
  - logical_source: GENERATOR_SOURCE
    host: node3:8080
    parser_config:
      type: CSV
    source_type: Generator
    source_config:
      generator_rate_type: FIXED
      generator_rate_config: emit_rate 10
      stop_generator_when_sequence_finishes: NONE
      seed: 1
      generator_schema: |
        NORMAL_DISTRIBUTION FLOAT64 0 1

workers:
  - host: node1:8080
    data_address: node1:9090
    max_operators: 10000
    peers:
      - node3:8080

  - host: node2:8080
    data_address: node2:9090
    max_operators: 10000

  - host: node3:8080
    data_address: node3:9090
    max_operators: 10000
    peers:
      - node2:8080
